// ИСХОДНИК подпутя `./built`. Его собранная пара (`dist/built.js`) отвечает другим словом, и
// на этой разнице проба различает, что именно отдал дев-сервер: правку соседа или его вчерашнюю
// сборку. Файл `dist` кладёт сама проба — под учётом его нет.

/** Откуда пришёл модуль. Значение в собранной паре — `"dist"`. */
export const origin = "src";
