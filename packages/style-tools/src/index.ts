// Поверхность инструментов стилизации. Самостоятельная НЕОБЯЗАТЕЛЬНАЯ поставка: оформление
// вправе взять эти инструменты у нас, взять у кого-то ещё или не брать вовсе (`PWEB-3`).
//
// Зависимости на набор значений (`@web-core/style`) тут нет и быть не может —
// инструменты не знают ни одного нашего токена. Обратной зависимости тоже нет: разрез идёт
// в обе стороны, иначе «необязательность» держалась бы обещанием.
//
// CSS отсюда не едет вовсе: инструменты работают с ИМЕНАМИ классов, а не со значениями.

export { cn } from "./cn.js";
export { createStyle, type VariantFn } from "./create-style.js";

// Реэкспорт CVA — точка, из которой потребитель берёт варианты, не заводя своей копии
// зависимости. Одна зависимость на продукт вместо одной на пакет.
export { cva, type VariantProps } from "class-variance-authority";
