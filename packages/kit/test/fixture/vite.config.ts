// Ровно то, что ляжет у потребителя: импорт, вызов, `export default`. Ни про solid-плагин,
// ни про версию Vite тут не знают — всё за точкой `/vite` (kb:PROBEWEB-2).
import { defineConfig } from "@omnifield/probe-web-kit/vite";

export default defineConfig();
