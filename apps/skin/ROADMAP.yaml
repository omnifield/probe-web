# Дорожная карта @web-core/skin-app — источник правды по фактически сделанной работе. Статус
# проставляется рядом с работой, в момент когда она сделана — не задним числом. Формат тот же,
# что у packages/store (categories → items, log → журнал решений); без milestones — у приложения
# нет освоенной поставки с датами релиза, только сделанное и открытое.

categories:
  - category: Структура каталогов
    items:
      - id: index-as-public-api
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: index.ts/tsx — единственный публичный вход папки
        description: |
          Означает: файл вида `папка/папка.tsx` (дублирующее имя) переименован в `папка/index.tsx`
          везде, где встретился (header, tree, preview, input, renderer, slot, app). Агрегирующие
          папки без единственного файла (model, ui, верх слайса entities/component,
          entities/showcase, widgets/component) получили тонкий index.ts-barrel — снаружи ходят
          только в него, не глубже.

      - id: index-stays-thin
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: index держит только реэкспорт, не логику
        description: |
          Означает: entities/component/utils/fake-generator разнесён на index.ts (один реэкспорт),
          generate.ts (сама функция), text.ts (приватные хелперы текста), hints.ts (таблица
          подсказок, тоже не публичная). Разбор — FAQ.md.

  - category: Импорты
    items:
      - id: no-file-extensions
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: Расширения убраны из всех импортов
        description: |
          Означает: ни настоящих (.ts/.tsx), ни поддельных под скомпилированный вывод (.js/.jsx) —
          бандлер (Vite, moduleResolution bundler) в них не нуждается.

      - id: hash-imports-need-tsconfig-paths
        type: bug
        status: Done
        priority: High
        milestone: null
        name: "package.json#imports (алиас #/*) не резолвится без расширения даже под bundler"
        description: |
          Означает: TypeScript 7.0.2 даёт мягкое резолвление bundler-режима (без расширения, с
          авто-index папки) только относительным путям и tsconfig.paths — не package.json#imports,
          тот остаётся строгим по спецификации Node ESM независимо от moduleResolution. Починено
          зеркальным compilerOptions.paths в apps/skin/tsconfig.json, локально для этого
          приложения. Разбор — FAQ.md.

  - category: Фейк-генератор (удалён)
    items:
      - id: schema-only-generator
        type: feature
        status: Removed
        priority: Medium
        milestone: null
        name: generateFakeData принимает io-схему, не компонент целиком
        description: |
          Было сделано так (движок один и общий — zocker, читает зод-теги прямо из схемы; второй
          параметр component — только ключ таблицы подсказок). Выпилено целиком решением user —
          см. `zocker-removed` в категории "Наполнение данными показа": наполнение теперь только
          из presets, зодер как источник данных показа больше не существует в этом приложении.

      - id: hints-per-field
        type: feature
        status: Removed
        priority: Medium
        milestone: null
        name: Таблица подсказок длины текста (component → field → length)
        description: |
          Было сделано так (supply() по ссылке на схему конкретного поля, не override() по типу).
          Выпилено вместе со всем `fake-generator/` — см. `zocker-removed`.

      - id: array-count-hints
        type: feature
        status: Removed
        priority: Medium
        milestone: null
        name: Подсказки количества/контента для полей-массивов
        description: |
          ЗАКРЫТО (2026-09-09) БЕЗ РЕАЛИЗАЦИИ — весь приём, которым эта задача собиралась
          пользоваться, снесён раньше неё. Означало (текст ТЗ, каким он был): для компонентов со
          списками (меню, аккордеон) — не просто заменить N пунктов на аналоги, а сгенерить и
          разное количество, и разный контент, тем же приёмом (`supply()` по ссылке на
          `.element`-схему массива + подсказка на количество), каким `hints-per-field` подставлял
          длину текста. Это `zocker`'s API — `fake-generator/` (генерация случайных данных по
          io-схеме) выпилен целиком решением user (`zocker-removed`): наполнение теперь только из
          сохранённых `content`-пресетов, автогенерации не существует вовсе, класть подсказку
          некому и незачем.

          Смысл задачи ("контролировать количество элементов массива") не исчез — переехал в
          `input-nested-fields-engine` (категория "Embed-маршруты"), но другим способом: не
          подсказка для генератора, а прямое добавление/удаление элементов в форме редактирования
          (человек или агент решает количество явно, кладя в `content`, а не движок гадает).

  - category: Документация
    items:
      - id: readme-app-template
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: README по общему шаблону — первый пилот genus "app"
        description: |
          Означает: Анатомия — таблица маршрутов (адрес = URL, не импорт-спецификатор, у
          приложения нет импортируемой поверхности); Использование — команды и адреса вместо
          кода-потребителя; Настройки — переменные окружения Vite и флаги dev-сервера; Состояния —
          статус componentInfoAtom (ResourceState); IO — параметры маршрута и адрес службы на
          входе, реальный рендер и фейковые данные на выходе; Рецепт — таблица подсказок
          фейк-генератора как съёмный слой снаружи io-схемы. genus "app" — новый, эталон для
          будущих apps/*.

      - id: faq-roadmap
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: FAQ.md и ROADMAP.yaml — первое заполнение
        description: |
          Означает: FAQ.md сгруппирован по темам (структура каталогов / импорты / фейк-генератор /
          тесты), каждый ответ со ссылкой на проверку. ROADMAP.yaml — по образцу packages/store
          (Done-пункты сразу с описанием, log — датированный журнал решений).

  - category: Наполнение данными показа
    items:
      - id: zocker-removed
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: "Фейк-генератор (zocker) выпилен — единственный источник данных теперь presets"
        description: |
          Означает: `entities/component/utils/fake-generator/` (generate.ts/hints.ts/payload.ts/
          text.ts) и неиспользуемый враппер `shared/utils/zocker/` удалены целиком, `zocker` убран
          из `package.json` и лок-файла этого приложения (в `apps/skin/.mcp` остаётся — отдельный
          пакет, отдельная зависимость, не трогали). `componentHandle.generate()` и его вызовы
          (кнопка "GENERATE FAKE DATA" в `Input`, fallback в `pages/embed/index.tsx` без
          `?content=`) убраны — компонент без сохранённого content теперь остаётся без данных
          (`embed`) или получает мок-строку (витрина, см. `input-content-select` ниже), не
          выдуманный зодером объект. Решение user: наполнение — только из наших пресетов.

      - id: input-content-select
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: "Input — выбор сохранённого content, дефолт на пункт 0, реакция на смену компонента"
        description: |
          Означает: `widgets/component/input` — контролируемый `Select` (`value={selected()}`, не
          отдан на откуп внутреннему стейту Ark) поверх `listContentFor(component)`. Эффект на
          `items()` сам выставляет пункт `0` и грузит его `state.data` через `component.setData`
          при каждой смене списка — в т.ч. при переключении компонента в дереве (баг: раньше
          подпись на триггере оставалась от предыдущего компонента, Select не реагировал). Список
          пуст → выбор сброшен, данные — литерал `"-мок-"`, не пустота и не зодер.

  - category: Слот показа
    items:
      - id: slot-loader-around-renderer
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: "Лоадер на время загрузки компонента — РОВНО вокруг Renderer, не вокруг слота"
        description: |
          Означает: `entities/showcase/ui/slot/loader.tsx` (`Loader`) подменяет ТОЛЬКО показываемое
          внутри `CarouselItem`; контрол сборок, тег, карусель со стрелками и индикаторами и её
          размер (`slotSizeOf`) остаются на экране — слот не схлопывается и не мигает. Признак
          ожидания слот сам не вычисляет: новый проп `loading?: boolean`, значение даёт вызывающий
          (`pages/showcase` — `componentHandle().loading()`), слот в стор не ходит.

          Источник признака — новый `loading()` в `ComponentHandle`
          (`entities/component/model/handle.ts`): `componentInfoAtom` в статусе `pending`. Это НЕ
          противоположность `ready()`: `createResourceAtom` держит последнее известное `data` и в
          `pending`, поэтому на смене компонента `ready()` остаётся true (info есть), но info это
          от ПРЕДЫДУЩЕГО компонента — показывать по нему нельзя. Отсюда отдельный флаг, а не
          `!ready()`.

          Своей графики у лоадера нет намеренно (решение user 2026-08-27, записано в `index.html`):
          витрина собрана из компонентов кита, одетых показываемым нарядом, а крутящегося спиннера
          в ките нет — ожидание сказано словом (`Typography` внутри `Flow`, центровка через
          `layoutGroup`/`layoutSelf` из `@web-core/skin`). Захочется вращающийся индикатор —
          это компонент КИТА (`packages/ui`), то есть чужая зона и отдельная заявка, а не своя
          анимация в витрине.

          Проверено живьём (dev-сервер + MCP-браузер, троттлинг "Slow 3G"): переключение
          button → table — "Загрузка…" стоит в области показа, контрол/карусель/размер на месте;
          после приезда info лоадер уходит, показ рисуется как прежде.

          Не покрыто (осознанно): ПЕРВЫЙ заход на компонент — `pages/showcase` рисует слоты
          `<For each={tagGroups()}>`, до приезда info список тегов пуст, слотов нет вовсе и
          показывать лоадер негде; это ожидание уровня СТРАНИЦЫ, а лоадер по ТЗ — только вокруг
          `Renderer`. Данные показа (`componentDataAtom`, сохранённый content) едут отдельно от
          info и этим флагом не покрыты — компонент, чей content ещё не выбран, лоадера не
          показывает.

  - category: Чат
    items:
      - id: chat-ui-split
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: "entities/chat/ui/chat — разбит на control/ и content/"
        description: |
          Означает: `control/index.tsx` — форма (инпут+кнопка отправки, весь стейт `text`/
          `onSubmit`); `content/item.tsx` — одно сообщение (аватар+бабл, свой/чужой); `content/
          container.tsx` — скролл-область и `For` по `messages()`. `chat/index.tsx` — тонкая
          сборка `ChatContent`+`ChatControl` внутри `Flow`, без своей логики. По ходу (решение
          user): имя автора и время сообщения убраны из `ChatItem` — остался только текст и
          аватар для выравнивания.

      - id: chat-real-backend
        type: feature
        status: Done
        priority: High
        milestone: null
        name: "Чат шлёт сообщения в реальную сессию агента, не в локальный мок"
        description: |
          Означает: новый `entities/chat/model/api.ts`'s `sendChatMessage(text)` —
          `POST {VITE_CHAT_URL}/sessions/{component}/messages` (дефолт URL —
          `https://150.251.145.87/api`), `Authorization: Bearer {VITE_CHAT_TOKEN}` (билд-тайм env,
          общий на всю витрину), `X-User-Login: {currentUser()}` (`entities/user`), тело
          `{text, context: {component}}`. Session id = имя ТЕКУЩЕГО компонента
          (`currentComponent`, `entities/component`) — по словам user, у агента чат хранится
          per-session, и сессия должна быть в рамках компонента; нет выбранного компонента → явный
          отказ, не запрос в никуда. Ответ — один синхронный JSON (`event: "run-finished"`), без
          стрима/поллинга.

          `store.ts`'s `sendMessage` пишет сообщение юзера в историю сразу (оптимистично), потом
          ждёт ответ и дописывает `reply` (или `refusal`, если он есть — тоже полноценный ответ
          агента, тем же путём). Сетевая ошибка/отказ службы тоже оседает в чат текстом, не
          проглатывается. Новый сигнал `pending` — `ChatControl` дизейблит форму на время запроса
          (сессия синхронная, второе сообщение до ответа на первое не имеет смысла). Моковая
          затравка (`MOCK_MESSAGES`, три сообщения "Ева"/"Архитектор") убрана — история теперь
          только из реальной переписки.

          Сознательно не сделано (не было источника во фронте на момент разговора): `variant`/
          `outfit` в `context` — их пока нигде не выбирают, context несёт только `component`;
          загрузка истории чата при заходе на компонент — эндпоинт на это не называли, `messages`
          стартует пустым; рендер `did` (списка вызовов MCP-тулов из ответа) — в UI сейчас виден
          только `reply`/`refusal`, `did` из ответа не читается вовсе.

          ЗАМЕНЕНО пунктом `chat-session-lifecycle` (ТЗ бокса, 2026-09-09): session id больше не имя
          компонента, а выданный боксом идентификатор, и ответ приходит не телом POST, а событием
          потока.

      - id: chat-session-lifecycle
        type: feature
        status: Done
        priority: High
        milestone: null
        name: "Сессия агента заводится на входе юзера и лежит в сторе рядом с логином"
        description: |
          Означает: сессию заводит НЕ чат и не первое сообщение, а вход юзера. `login()`
          (`entities/user/model/store.ts`) пишет логин и тут же зовёт `POST /sessions`
          (`{recipe, passport, agent, title}`, `title` — логин: компонента в этот момент ещё нет);
          выданный боксом id ложится рядом с логином — сигнал `currentSession` и localStorage-ключ
          `session` рядом с `user`. Оба живут одной жизнью: сессия видна только своему логину (чужая
          отвечает 404, как несуществующая), поэтому `logout()` чистит и то, и другое.

          Кому нужен id или логин — берёт их из стора юзера (`currentSession()`/`currentUser()`),
          из любого места. Чат ничего не хранит: подставляет их в каждый запрос и всё. Единственное
          исключение — бокс ответил 404 (сессии больше нет): чат просит стор завести новую
          (`renewSession()`) и повторяет ход ОДИН раз, чтобы обрыв на стороне бокса не съедал
          сообщение юзера.

          Транспорт к боксу переехал в `shared/api/box` — адрес, токен, заголовки, `createSession`.
          Он общий для двух сущностей (`entities/user` заводит сессию, `entities/chat` в неё пишет),
          и лежит в shared именно поэтому: иначе одна сущность импортировала бы другую ради адреса.

          `POST /sessions/{id}/messages` отдаёт 202 и КВИТАНЦИЮ (`run.id`), а не ответ агента —
          агент думает от 20 секунд до минут, столько соединение не живёт. Ответ забирается из
          `GET /sessions/{id}/events` (SSE с заголовками, поэтому браузерный `EventSource` не
          годится — обычный `fetch` с чтением тела потоком). Поток открывается ДО POST, чтобы не
          упустить конец быстрого прогона, и ждёт `run-finished` ИМЕННО своего `run` — чужие события
          сессии пропускаются. Оборвался поток — прогон от этого не останавливается: дочитывание
          идёт через `GET /runs` (состояние), `GET /messages` (реплика с `author: "agent"` и нашим
          `run_id`) и `GET /runs/{run}/steps` (наблюдённые вызовы ручек). Имя компонента едет в
          `context` сообщения — бокс его не разбирает и передаёт агенту как данные.

          Заголовки `Authorization: Bearer` и `X-User-Login` — на КАЖДОМ запросе, без любого из двух
          бокс отвечает 401; поэтому не залогинен → запроса нет вовсе, а не 401 от бокса. Логин
          кириллицей едет процентно закодированным: HTTP-заголовок — байты (ISO-8859-1), и такой
          логин роняет сам `fetch` ещё до сети (поймано тестом `test/chat-session.test.ts`).
          Потолок ожидания прогона — 10 минут: без него оборванный на той стороне прогон держал бы
          форму задизейбленной вечно. Бокс недоступен в момент входа — юзер всё равно вошёл (витрина
          живёт и без чата), ошибка видна тостом, сессия заведётся при первом сообщении.

          Имена рецепта/паспорта/агента — env (`VITE_CHAT_RECIPE`/`VITE_CHAT_PASSPORT`/
          `VITE_CHAT_AGENT`, дефолты `сборка-скинов`/`опус-5`/`claude-code`): бокс на неизвестное имя
          отвечает 400 с текстом, актуальные списки — `GET /api/catalog/recipes`,
          `/api/catalog/passports`, `/api/agents`.

  - category: Embed-маршруты
    items:
      - id: embed-routes-for-agent-playground
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: "Embed-маршруты — изолированный playground для агента, не живая витрина юзера"
        description: |
          Цепь: `apps/skin/.mcp` (MCP-сервер) уже даёт внешнему агенту headless-браузер со своей
          вкладкой на сессию (`browser_navigate`/`browser_snapshot`/`browser_click`/
          `browser_screenshot`) — изоляция ПРОЦЕССА/вкладки решена. Но `browser_navigate` ходил на
          живые маршруты витрины (`/showcase/...`), а там хедер/чат/сайдбар и прочий UI, не
          относящийся к делу. Кейс "агент работает со сборками" — по словам user, "не частый кейс,
          но не одноразовый" — этому шум прямо мешал.

          User дословно: "я не хочу чтобы агент читал саму витрину, в которой работает юзер, так
          как там много шума. поэтому нам для скина нужно поднять свой [плейграунд]. или как
          вариант сделать отдельные маршруты embed что бы агент мог брать изолированно".

          Решение (обсуждено и принято) — ВТОРОЙ вариант, не отдельное приложение:
          - Отдельный процесс/приложение дублировал бы рендер-механику, которая уже есть и работает
            в этой витрине (`Renderer`/`instanceOf`, подключение скина, `componentDataAtom`) — риск
            разъехаться с оригиналом, лишняя инфраструктура на поддержку.
          - Изоляция ПРОЦЕССА не нужна повторно — она уже дана MCP-браузером (своя вкладка на
            сессию, агент физически не пересекается с вкладкой живого юзера). Нужна изоляция
            DOM/визуального шума, не вторая инфраструктура — для этого достаточно маршрута БЕЗ
            хрома приложения в ТОМ ЖЕ бандле.
          - Тот же приём у Storybook (canvas в отдельном iframe того же приложения, не второй
            сервер) и Ladle/Histoire.

          Сделано: новый маршрут `/embed/$component/$assembly` (`routes/embed.$component.$assembly.tsx`
          + `pages/embed/index.tsx`) рендерит ТОЛЬКО `<Renderer>` — без `Header`/чата/сайдбара.
          Чтобы дать ему путь мимо хрома, хром (`WorkspaceLayout`) переехал из `__root.tsx` в
          отдельный pathless layout `_workspace.tsx` (`createFileRoute("/_workspace")`) — все
          прежние маршруты (`/`, `/lab`, `/playground`, `/showcase/...`) стали его детьми
          (файлы переименованы с префиксом `_workspace.`, URL не изменились), `/embed/...` остался
          сиротой прямо под корнем и хром не наследует. Источник данных — `?content=<name>` (новая
          `getContentByName` в `entities/component/model/content.ts`, читает ту же службу пресетов,
          что и `get_content` в `apps/skin/.mcp`), без параметра или если имя не найдено —
          существующий fallback (`generateFakeData`). Проверено вручную (dev-сервер + MCP-браузер):
          `/embed/button/base` — фейковые данные; `/embed/button/base?content=button-save` —
          сохранённый content; `/showcase/button` — хром не сломан.

          Дальше по цепи (не в этом файле): `apps/skin/.mcp`'s `browser_navigate`/доки (`docs/*.md`)
          должны научиться указывать на `/embed/...` для кейса сборок вместо `/showcase/...` — это
          отдельный, более мелкий заход со стороны MCP.
      - id: input-widget-real-ui
        type: feature
        status: Done
        priority: Medium
        milestone: null
        name: Input — редактирование отдельных полей данных, не только выбор целого набора
        description: |
          Означает: выбор ГОТОВОГО набора данных (сохранённый content, дефолт на пункт 0) уже
          сделан — см. `input-content-select`. Не сделано было — редактирование ОТДЕЛЬНЫХ полей
          выбранного набора (форма по io-схеме компонента, поле за полем); единица изменения была
          весь набор целиком, точечная правка одного поля была невозможна без ухода в службу
          пресетов напрямую.

          ПЕРВЫЙ ЗАХОД (согласовано с user 2026-09-09 до кода): скаляры (string/number/boolean/
          enum строк) io-схемы — верхний уровень и ОДИН уровень вложенных объектов; массивы и
          более глубокая вложенность НЕ рендерятся вовсе (движок под них — отдельное ТЗ,
          `input-nested-fields-engine` ниже).

          Сделано: `widgets/component/input/schema.ts` — `fieldsOf(schema)` через `z.toJSONSchema`
          (официальный экспорт zod 4, не обход `_def`-внутренностей — та же форма, что библиотека
          поддерживает как контракт) с `unrepresentable: "any"` (схема компонента — внешняя
          граница, не всё представимо JSON Schema, например `z.unknown()`, это законный случай, не
          авария); `valueAt`/`withValue` — чтение/иммутабельная запись по пути глубиной 1–2 (свои,
          не `assign`/`lookup` из `@web-core/io` — та функция МУТИРУЕТ, для сигналов Solid нужна
          новая ссылка на каждом уровне). `widgets/component/input/fields.tsx` — `DataFields`,
          контрол на `kind` (`BooleanField`/`Checkbox`, `EnumField`/`Select`, `ScalarField`/
          `FieldInput` text-или-number) через `Switch`/`Match`, не if/early-return (Solid-компонент
          выполняется один раз — `solid/components-return-once` поймал первую версию на ревью
          линтом). `ComponentHandle` (`entities/component/model/handle.ts`) получил `data(): unknown`
          — геттер поверх `componentDataAtom` (`useAtom`, тот же приём, что `info`/`loading`) —
          раньше был только `setData`, а точечной правке нужно читать ТЕКУЩЕЕ значение, чтобы
          класть новое поверх него, не поверх записи из `records`.

          Проверено живьём (свой dev-сервер 5175, `PRESETS_URL` переопределён на локальный GraphQL-
          бэк — деталь окружения этой сессии, не постоянная правка): `/showcase/button` — под
          селектом наборов появились `label`/`data-variant` (текстовые), `payload: z.unknown()`
          корректно не отрисован. Boolean/enum-ветки и вложенный уровень — код проверен изолированно
          (`z.toJSONSchema` на тестовых схемах вручную, TypeScript+ESLint чисто), НЕ кликом в
          браузере: ни один компонент в ките сегодня не использует `z.boolean()`/`z.enum()` в
          io-схеме (`grep` по всем `entity/io.ts` — пусто), протестировать эти ветки живым
          компонентом не на чем.

          НАХОДКА ПО ХОДУ (инфраструктура, не код этой зоны): удалённый бэк по умолчанию
          (`PRESETS_URL=https://150.251.145.87:9787` в корневом `.env`) отвечает на `/graphql`
          404-ошибкой СТАРОГО REST-формата (`{"error":"not_found","message":"Такого пресета нет."}`)
          — это НЕ новый Go+GraphQL бэк из `backend/presets` (тот на 404 отвечает GraphQL-конвертом
          `{"errors":[...],"data":null}`, проверено на локальном инстансе). Разбор `/api/presets`
          на том же хосте отвечает как раньше (REST жив, 49 записей) — деплой на этом хосте не
          обновлён до переезда на GraphQL, хотя код в репозитории уже полностью на GraphQL
          (`shared-api-presets-single-endpoint` и раньше). Значит ЛЮБОЙ потребитель с адресом по
          умолчанию (не только apps/skin) сегодня не может читать пресеты вовсе. Не чинил (не моя
          зона — деплой/инфра), нужно решение user/architect.

      - id: input-nested-fields-engine
        type: feature
        status: Backlog
        priority: High
        milestone: null
        name: "Input — движок для вложенности: массивы и глубина >1 в io-схеме"
        description: |
          Заведено по ходу `input-widget-real-ui` (2026-09-09, ТЗ user): первый заход того пункта
          намеренно закрывает только скаляры верхнего уровня и одного уровня вложенных объектов —
          массивы и вложенность глубже одного уровня не рендерятся вовсе (`schema.ts`'s `fieldsOf`
          их пропускает молча).

          УТОЧНЕНИЕ (2026-09-09, после `unified-item-collection-engine` в `packages/ui/ROADMAP.yaml`):
          пример был устаревшим уже в момент написания — `accordion`'s `sections[].items[]` больше
          не существует, весь кит переведён на ОДИН канонический рекурсивный `item`
          (`shared/data/fields.ts`, `{value, label, children?: item[]}`). Это меняет объём задачи в
          выгодную сторону: движок редактирования массива нужен НЕ по одной реализации на каждый
          компонент (`accordion`/`tabs`/`listbox`/`select`/`tree-view`/`menu`/`radio-group`), а ОДИН
          рекурсивный редактор `item[]` — плоские компоненты просто не показывают контрол
          "добавить вложенный элемент" для листовых узлов (или показывают, если `children`
          разрешён и там, где до сих пор не запрещался явно).

          Полноценный редактор массива — это не «ещё один `kind`» в существующем списке: нужно
          добавлять/убирать/переставлять элементы, у каждого элемента — своя вложенная форма (та же
          `fieldsOf`, рекурсивно по `children`), значит другая архитектура компонента (список форм,
          не строка полей), не расширение `DataField`'а. Компоненты кита СВЕРХ канона (`tabs`'s
          `content`, `accordion`'s `activeValues`) — та же рекурсивная форма `fieldsOf` для полей
          ОДНОГO элемента массива, отдельная задача не нужна, это уже покрыто `input-widget-real-ui`
          на уровне одного элемента — новое здесь только сам список (add/remove/reorder) и
          рекурсия в `children`.

      - id: lab-page-purpose
        type: feature
        status: Backlog
        priority: Low
        milestone: null
        name: "/lab — назначение экрана не объявлено"
        description: |
          Означает: маршрут жив ("Лаборатория (мок)"), что там будет показано — не решено.

      - id: no-tests
        type: feature
        status: Done
        priority: High
        milestone: null
        name: "Единственный интеграционный тест бил через мёртвый код — почищено"
        description: |
          Было (текст ТЗ устарел ещё до открытия — проверено ДО правок): `test/` не пустая, в ней
          уже пять файлов, четыре проходили. Пятый (`input-to-preview.test.tsx`, «автогенерация на
          смену компонента доезжает до показа текстом») падал: `widgets/component/preview` (его
          единственный потребитель) звал `<Slot slides={[...]}>` — устаревший контракт, `Slot`
          (`entities/showcase/ui/slot`) давно принимает `component`/`tag`/`assemblies`/`variants`/
          `loading` (переехал ещё в `slot-loader-around-renderer`), реальная витрина
          (`pages/showcase`) зовёт именно так. `widgets/component/preview` нигде в коде больше не
          импортировался (только реэкспорт из барреля) — мёртвый код, живший исключительно ради
          этого теста. Название теста прямо называло причину: он проверял фейк-генератор
          (`zocker`), которого больше нет (`zocker-removed`).

          Остальные четыре теста — три узких регрессионных теста ДВИЖКА (`RenderTree`/
          `RenderNode`, `@web-core/assembly`, голым API без виджетов приложения) и один про
          процентное кодирование логина в заголовке чата (`chat-session.test.ts`) — валидны,
          не тронуты. Ни один не проверял РЕАЛЬНЫЙ путь через актуальный `Slot`, который README
          называет "Сборки… проверены только вручную".

          Сделано: `widgets/component/preview` снесён целиком (директория + строка в барреле
          `widgets/component/index.ts`) — согласовано с user до правки. `input-to-preview.test.tsx`
          переписан на актуальный контракт `Slot` (тот же, каким его зовёт `pages/showcase`), два
          случая: (1) сохранённый content-рекорд долетает до видимого текста показа
          (`data.label` → `/label`-биндинг паспорта button) — то, что README называет проверенным
          только руками, теперь проверяется прогоном; (2) пустой список content не роняет показ
          (мок-строка `"-мок-"`, которую `Input` кладёт при пустом списке, НЕ рендерится текстом —
          у button биндинг `/label`-путём, скаляр вместо объекта не находит поле, значит проверять
          "текст не пуст" для этого случая было бы неверно — тест проверяет только "не упало").
          Сеть не трогали: `@web-core/skin/presets`'s `createPresetsClient` замокан целиком
          (`vi.mock` + `vi.hoisted` под мутируемое состояние моков между двумя `it`), настоящие
          `componentInfo`/`Input`/`Slot` — без изменений, граница мока — ровно на сетевом клиенте.
          `queryClient.clear()` в `afterEach` — кэш `content-graphql-migration` иначе пережил бы
          между двумя `it` в одном файле и второй тест видел бы данные первого.

  - category: GraphQL-переезд (третий шаг цепи backend/presets → packages/query → потребители)
    items:
      - id: shared-api-presets-single-endpoint
        type: feature
        status: Done
        priority: High
        milestone: null
        name: "shared/api/presets — один адрес /graphql, не сборка ?kind=/id"
        description: |
          Означает: `resolveUrl()` (`shared/api/presets/index.ts`) дописывала `API_PATH =
          "/api/presets"` и комментарием ссылалась на константу `root` в
          `backend/presets/internal/api/handler.go` — файла уже не было, ссылка была мёртвой.
          Заменено на `GRAPHQL_PATH = "/graphql"` (путь держит бэк константой в
          `mux.Handle("POST /graphql", ...)`, `backend/presets/cmd/presets/main.go`), логика
          суффиксации та же (полный адрес с путём принимается как есть, без пути — дописывается).
          `PRESETS_URL`/`VITE_PRESETS_URL` как переопределение не тронуты — один адрес на всё
          приложение, не три копии; трое потребителей (`entities/component/model/info.ts`,
          `.../content.ts`, `shared/ui/theme-switch`) правки не потребовали — все передают
          `PRESETS_URL` как есть в `createPresetsClient({url})`/`createPresetsSkinSource({url})`.

          По ходу найдено (не в этом ТЗ, не чинено): `apps/skin/.mcp/src/engine/store.ts` — свой,
          отдельный REST-клиент с той же мёртвой ссылкой (`API_PATH = "/api/presets"`), плюс
          `.mcp/README.md` и `.mcp/FAQ.md` документируют старые REST-примеры. `.mcp` — отдельный
          пакет (см. `zocker-removed`), отдельное ТЗ.

      - id: content-graphql-migration
        type: feature
        status: Done
        priority: High
        milestone: null
        name: "entities/component/model/content.ts — кэш на сессию, транспорт уже GraphQL"
        description: |
          ИСПРАВЛЕНО (2026-09-09, найдено овнером ДО написания кода — версия ниже отменяет
          первую): исходный текст этого пункта описывал «REST-клиент → graphqlRequest напрямую»,
          но писался ДО того, как `packages/skin`'s `createPresetsClient` сам переехал на GraphQL
          (`client-graphql-transport`, коммит `4f359e6`) — тот самый клиент, которым этот файл уже
          пользуется. Транспорт здесь уже GraphQL, вопрос был решён в другой зоне, и текст ТЗ этого
          не знал.

          Реальный оставшийся пробел — только кэш на сессию, которого правда не было: каждый заход
          в компонент бил по сети заново (`listContentFor`/`getContentByName` звали
          `presets.list`/`presets.get` без кэша). Решение — НЕ заводить второй, дублирующий
          GraphQL-запрос и свою конверсию ответа (это уже есть и протестировано в
          `client.ts`'s `LIST_QUERY`/`toRecord`), а обернуть существующие вызовы `presets.list`/
          `presets.get` в `queryClient.fetchQuery({..., staleTime: Infinity})` из
          `@web-core/query`. `fetchQuery` — императивный метод клиента, не хук `createQuery`: этот
          файл не Solid-компонент, `createQuery` сюда физически не встал бы.

          `staleTime: Infinity` — намеренно, не заглушка: авто-протухание по времени не нужно,
          если инвалидация явная и событийная (`chat-did-cache-invalidation` — по сигналу из
          `did`, не по таймеру). Два механизма протухания разом (время + событие) были бы лишней
          сложностью без выгоды.

          Сделано: новый `#/shared/api/query-client` (`export const queryClient = new
          QueryClient()`) — общий инстанс СНАРУЖИ `app/index.tsx` (тот его раньше заводил сам и не
          отдавал наружу), нужен и `content.ts`, и будущему `chat-did-cache-invalidation` вне
          Solid-дерева, где `useQueryClient()` не работает. `listContentFor` кэширован ключом
          `["content", "list"]`, `getContentByName` — `["content", "get", name]`, отдельной
          записью, не через список (буквально по тексту ТЗ — обернуть оба вызова, не только
          `list`). Находка по ходу: query-core запрещает `queryFn` возвращать `undefined`
          (означает «данных ещё нет», не «записи нет»), а «имя не найдено» у content — законный
          исход (`/embed/...` без `?content=`) — `getContentByName` кэширует `null` вместо
          `undefined` и разворачивает обратно на выходе.

      - id: chat-did-cache-invalidation
        type: feature
        status: Done
        priority: High
        milestone: null
        name: "Ответ агента триггерит перезапрос — chat.did уже приходит, просто не читается"
        description: |
          Заявка user: когда агент через MCP меняет пресет (`save_preset`/`save_content`), витрина
          должна САМА перезапросить задетые данные, не только показать ответ в чате. Расширять MCP
          не нужно — у него нет канала в браузер, канал уже есть: `ChatRunResult.did` (`entities/
          chat/model/api.ts:27`) на каждом `run-finished` несёт `{server, tool, arguments}` по
          каждому вызову MCP-тула за прогон. Сейчас читается только `reply`/`refusal` — `did`
          сознательно не читался (см. `chat-real-backend` выше), этим заходом дочитывается.

          ИСПРАВЛЕНО (2026-09-09, найдено овнером ДО написания кода — версия ниже отменяет
          первую): исходный текст велел вытаскивать `arguments.state?.name`/
          `arguments.state?.component` и инвалидировать по ним. Но реальный кэш, который завёл
          `content-graphql-migration`, — ключи `["content", "list"]`/`["content", "get", name]`,
          НЕ по компоненту (`listContentFor` фильтрует список на клиенте уже ПОСЛЕ чтения из
          кэша) — ключа вида `["content", component]` в коде нет. Для `form`/`outfit`/`palette`/
          `assembly` (виды `save_preset`) кэша в этом приложении вообще нет —
          `componentInfo`/`packages/ui` на `queryClient` не переехал (снятый отсюда кросс-зонный
          блокер, см. лог ниже). Вытаскивать `name`/`component` сейчас смысла не было — им
          некуда деваться, ни один кэш по ним не ключуется.

          Механика (по факту, не по гипотезе): `sendChatMessage` возвращает `did` — на успешном
          ответе агента пройти по `did`; `tool === "save_content"` →
          `queryClient.invalidateQueries({queryKey: ["content"]})` (TanStack Query матчит по
          ПРЕФИКСУ ключа — накрывает и `list`, и `get`, весь список целиком, без разбора по
          компоненту: дешёвый лишний перезапрос вместо пропущенного); `tool === "save_preset"` →
          `queryClient.invalidateQueries({queryKey: [arguments.kind]})` — сегодня no-op (нечего
          инвалидировать), но задел под конвенцию ключей на будущее, когда `componentInfo`
          переедет на `queryClient`.

          Заявка на форму `did.arguments` ЗАКРЫТА (2026-09-09, ответ box-агента): правило
          структурное, не по именам полей — скаляры доживают до `did` на любой глубине ДО второй
          включительно, объёмные (объекты/массивы глубже) заменяются пометкой размера. Живой
          прогон, пример для `save_preset`:

          ```json
          "save_preset": {
            "kind": "form",
            "state": {
              "name": "proba-did-form", "component": "button",
              "recipe": "«объект из 4 полей»", "variantTags": "«объект из 8 полей»"
            }
          }
          ```

          Значит `arguments.kind`/`arguments.state?.name`/`arguments.state?.component` — ВСЕГДА
          настоящие значения, не пометки (это скаляры на глубине ≤2), а `state.recipe`/`.assembly`/
          `.scales`/… — строка-пометка, не объект: код инвалидации не должен пытаться читать ИЗ
          них, только проверять их отсутствие/наличие как сигнал вида. Правило общее для любых
          будущих ручек ("так это будет работать и с теми ручками, которых ещё нет" — дословно
          box-агент), не привязано к словарю имён этой заявки — не нужно синхронизировать список
          полей при появлении новых kind. Проверено локально агентом на его стороне; на сервере
          прогон не повторялся (платный) — если нужна проверка там, решать user.

          Требует `@web-core/query`'s `QueryClient` смонтированным в приложении, чтобы у
          `invalidateQueries` было что инвалидировать — см. `content-graphql-migration` (первый
          настоящий потребитель `createQuery` в этом приложении, `finding-no-cache` в корневом
          ROADMAP.yaml уже отмечал, что `QueryClientProvider` смонтирован, но не использован).

          Сделано: `entities/chat/model/store.ts`'s `invalidateFor(did)` — по каждому вызову из
          `ChatRunResult.did`, `save_content` дёргает `queryClient.invalidateQueries({queryKey:
          ["content"]})` (совпадение по ПРЕФИКСУ — накрывает и `list`, и `get`), `save_preset`
          дёргает тем же способом по `arguments.kind` (сегодня no-op, задел на будущее). Зовётся из
          `sendMessage` на КАЖДОМ успешном ответе `sendChatMessage` (и `reply`, и `refusal` —
          `did` не привязан к исходу, тулы могли быть вызваны до отказа), не только когда есть
          текстовый `reply`.

      - id: witrina-remote-graphql-live
        type: feature
        status: Done
        priority: High
        milestone: null
        name: "Подключить витрину к внешнему стенду — деплой подтверждён живьём"
        description: |
          Закрывает находку `input-widget-real-ui-first-pass` (лог ниже): удалённый стенд БЫЛ на
          старом REST, теперь раскатан на GraphQL и проверен внешне (2026-09-09, отчёт после
          `ship.ps1`): `POST https://150.251.145.87:9787/graphql` отвечает, старый
          `/api/presets` — 404, `/healthz` наружу оставлен. Живой прогон настоящим агентом
          (прочитал группу компонентов → io-схему → `save_preset`) подтвердил и
          `author-header-not-argument` (см. `apps/skin/.mcp/ROADMAP.yaml`) на реальном контуре:
          `author` записи — логин из заголовка (`Raybul`), не то, что мог напечатать агент.

          Дальше — то, ради чего всё это: витрина переходит с локального бэка на этот адрес для
          РЕАЛЬНОЙ работы (создание скинов), не только для теста. `PRESETS_URL`/
          `VITE_PRESETS_URL` (`shared/api/presets`, `.env.local` — не в гит) →
          `https://150.251.145.87:9787/graphql`. CORS на `localhost:5174` уже открыт на бэке —
          проверять с этой стороны нечего, только выставить адрес и убедиться живым запросом
          из браузера.

          Локальный бэк+`.mcp` (тот, что использовали для разработки этой миграции) остаётся —
          для разработки и тестов функционала внутри репозитория, не для создания реальных
          скинов. Внешний контур — отдельная, параллельная площадка: там же тестируются внешние
          агенты и их функционал, не только витрина.

          Старые записи на внешнем стенде несут разнобой в `author` (`Egor Raybul` /
          `egorraybul@gmail.com`) — следы прежних, REST-эры заливок, не сегодняшней миграции;
          не чинить задним числом, только новые записи идут через заголовок.

          Сделано: НИКАКОЙ правки не потребовалось — корневой `.env` воркспейса (машинный, не в
          гит) уже держал `PRESETS_URL=https://150.251.145.87:9787` ДО этой сессии (тем самым
          адресом и была найдена находка `input-widget-real-ui-first-pass` — старый бэк на нём
          отвечал REST-404). Раз бэк переехал, тот же адрес автоматически стал верным: `resolveUrl`
          (`shared-api-presets-single-endpoint`) сам дописывает `/graphql`, вторая правка не нужна.

          Проверено живьём (свой dev-сервер 5175, БЕЗ переопределений): `/showcase/button` —
          наряд `omnifield` надет полностью (стили, не голый HTML), выпадашка "Наши данные"
          показала настоящую сохранённую запись, поля `label`/`data-variant`
          (`input-widget-real-ui`) заполнились её реальными значениями, показ отрисовал кнопку
          этими данными в двух сборках (default/status). Реальный сквозной прогон через GraphQL,
          не заглушка.

          НЕ СДЕЛАНО (вне зоны): пункт просил `.env.local` — отдельный, независимый от общего
          `.env` пин на случай, если кто-то переключит общий файл обратно на локальный бэк для
          разработки. Технически такой файл должен лежать в КОРНЕ воркспейса
          (`/workspaces/probe-web/.env.local`), не в `apps/skin/` — `envDir` этого приложения
          (`packages/build/src/vite/app.ts`) жёстко указывает на корень воркспейса, файл ВНУТРИ
          `apps/skin/` Vite не читает вовсе (проверено — завёл такой по ошибке, `loadEnv` его не
          увидел, убрал). Корень воркспейса — не моя зона (`apps/skin/`), создавать там файл не
          стал; раз действующий `.env` и так уже указывает правильно и живой прогон это
          подтвердил, отдельный пин можно завести отдельным мелким шагом через architect, если
          нужна такая страховка.

log:
  - id: array-count-hints-obsolete
    type: plan
    date: 2026-09-09
    summary: >
      `array-count-hints` закрыт без реализации: его приём (`supply()` по `.element`-схеме
      массива + подсказка на количество для фейк-генератора) — API `zocker`, выпиленного целиком
      (`zocker-removed`). Наполнение теперь только из сохранённых `content`, автогенерации нет —
      подсказку класть некому. Перенесён в категорию "Фейк-генератор (удалён)" (третий пункт рядом
      со `schema-only-generator`/`hints-per-field`, тем же основанием). Смысл задачи не пропал —
      контроль количества элементов массива переехал в `input-nested-fields-engine`, но другим
      способом: прямое добавление/удаление в форме редактирования, не подсказка генератору.

  - id: dead-preview-widget-removed-real-test
    type: plan
    date: 2026-09-09
    summary: >
      `no-tests` оказался устаревшим ещё до открытия: `test/` не пустая, четыре из пяти тестов валидны,
      пятый падал из-за мёртвого кода. `widgets/component/preview` звал `Slot` устаревшим
      контрактом (`slides`), реальная витрина давно зовёт актуальным (`component`/`tag`/
      `assemblies`/`variants`/`loading`, переезд ещё в `slot-loader-around-renderer`) — виджет не
      импортировался нигде, кроме этого теста, и сам тест проверял снесённый фейк-генератор
      (`zocker-removed`). Снёс виджет (директория + строка барреля), переписал тест на актуальный
      `Slot` с двумя случаями: контент долетает до текста показа (реальный путь, который README
      числил проверенным только руками) и пустой список не роняет показ. Сеть замокана точечно —
      `createPresetsClient` через `vi.mock`/`vi.hoisted`, сама логика `componentInfo`/`Input`/
      `Slot` настоящая. `queryClient.clear()` в `afterEach`, иначе кэш `content-graphql-migration`
      тёк между двумя `it` одного файла.

  - id: remote-stand-shipped-and-verified
    type: plan
    date: 2026-09-09
    summary: >
      Закрывает находку `input-widget-real-ui-first-pass` ниже (удалённый стенд по умолчанию был
      на старом REST). User прогнал `ship.ps1`: пуш `dev` → подтяжка на машине → пересборка
      `presets`+`skin-mcp` из `dev` → подъём → перечитан вход → проверки. Отчёт: старый
      `/api/presets` — 404, `POST /graphql` отвечает 200 и с 9787 наружу тоже; `skin-mcp` поднялся
      с адресом `http://presets:8787` без REST-пути (внутренняя сеть контейнеров, не публичный
      хост); `/healthz` оставлен наружу, как договаривались. Живой прогон настоящим внешним
      агентом (не эмуляция) — прочитал группу компонентов, взял io-схему, сохранил `proba-graphql`
      (`author=Raybul`, `component=button`) — подтвердил `author-header-not-argument` на реальном
      контуре: имя пришло заголовком, не аргументом тула. Старые записи несут разнобой автора
      (`Egor Raybul`/`egorraybul@gmail.com`) — следы прежних REST-заливок, не сегодняшней работы,
      не тронуты. CORS на `localhost:5174` открыт для будущего подключения витрины —
      см. `witrina-remote-graphql-live` выше, следующий шаг.

  - id: input-widget-real-ui-first-pass
    type: plan
    date: 2026-09-09
    summary: >
      `input-widget-real-ui`, первый заход (объём согласован с user до кода: скаляры верхнего
      уровня + один уровень вложенных объектов, массивы — отдельное ТЗ `input-nested-fields-engine`).
      `widgets/component/input/schema.ts` — `fieldsOf` через официальный `z.toJSONSchema` (zod 4),
      не приватные `_def`-внутренности; `valueAt`/`withValue` — свои, иммутабельные (не `assign` из
      `@web-core/io` — та мутирует). `fields.tsx` — `DataFields`/`DataField` с `Switch`/`Match`
      (early-return в компоненте поймал `eslint-plugin-solid`: `solid/components-return-once`).
      `ComponentHandle` получил `data()` — геттер поверх `componentDataAtom`, раньше был только
      `setData`.

      Проверено живьём на `/showcase/button` (свой dev-сервер 5175): поля `label`/`data-variant`
      появились, `payload: unknown` не отрисован. Boolean/enum-ветки — только изолированным тестом
      `z.toJSONSchema`, не кликом: в ките сегодня НИ ОДИН компонент не использует `z.boolean()`/
      `z.enum()` в io-схеме.

      Находка по ходу (эскалирована user, не чинил — не моя зона): удалённый бэк по умолчанию
      (`150.251.145.87:9787`, корневой `.env`) отвечает на `/graphql` старым REST-404
      (`"Такого пресета нет"`), не GraphQL-конвертом — деплой на этом хосте не переехал на GraphQL,
      хотя код репозитория уже переехал. Проверка велась с `PRESETS_URL`, переопределённым на
      локальный GraphQL-инстанс (деталь окружения сессии).

  - id: chat-did-cache-invalidation
    type: plan
    date: 2026-09-09
    summary: >
      Третий, последний шаг GraphQL-цепочки. Текст ТЗ велел вытаскивать `arguments.state?.name`/
      `.component` из `did` и инвалидировать по ним — сверка с реальным кэшем (заведённым только
      что в `content-graphql-migration`) показала, что ключи там не по компоненту (`["content",
      "list"]`/`["content", "get", name]` — фильтр по компоненту на клиенте, ПОСЛЕ чтения из
      кэша), а кэша под `form`/`outfit`/`palette`/`assembly` в приложении вообще нет
      (`componentInfo` на `queryClient` не переехал). Вытаскивать поля, которым некуда деваться, не
      стал — согласовано с user до кода.

      `invalidateFor(did)` в `entities/chat/model/store.ts`: `save_content` бьёт по всему ключу
      `["content"]` (префиксное совпадение TanStack Query накрывает оба под-ключа content разом),
      `save_preset` — по `arguments.kind` тем же способом (сегодня no-op, но конвенция ключей
      готова под будущий GraphQL-клиент `componentInfo` без правки этого файла). Вызывается из
      `sendMessage` на каждом успешном прогоне, не только при непустом `reply`.

  - id: content-graphql-migration-session-cache
    type: plan
    date: 2026-09-09
    summary: >
      Второй шаг GraphQL-цепочки (`content-graphql-migration`). Текст ТЗ описывал переезд
      `content.ts` с REST-клиента на прямой `graphqlRequest` — сверка с кодом ДО реализации
      показала, что транспорт уже GraphQL (`packages/skin`'s `createPresetsClient` мигрировал
      раньше, коммит `4f359e6`), реальный пробел — кэш на сессию, которого не было вовсе. Решение
      согласовано с user: обернуть существующие `presets.list`/`presets.get` в
      `queryClient.fetchQuery({staleTime: Infinity})`, не дублировать GraphQL-запрос/конверсию,
      уже готовые в `client.ts`.

      Заведён `#/shared/api/query-client` — единственный `QueryClient` на приложение, вынесенный
      из `app/index.tsx` наружу: нужен вне Solid-дерева (`content.ts` — обычные async-функции, не
      компонент; `useQueryClient()` там не работает), и понадобится тем же способом
      `chat-did-cache-invalidation`. `listContentFor` кэширован ключом `["content", "list"]`,
      `getContentByName` — отдельным `["content", "get", name]` (буквально по ТЗ — обёрнуты оба
      вызова, не только список).

      Найдено и почитано по ходу (не в тексте ТЗ, техническая необходимость библиотеки): query-core
      не принимает `undefined` из `queryFn` (зарезервировано под «данных ещё нет») — `getContentByName`
      кэширует `null` вместо `undefined` на случай ненайденного имени (законный исход, не ошибка) и
      разворачивает обратно перед возвратом вызывающему.

  - id: shared-api-presets-graphql-endpoint
    type: plan
    date: 2026-09-09
    summary: >
      Первый шаг GraphQL-цепочки (`shared-api-presets-single-endpoint`): `shared/api/presets`
      собирала базу REST-путём `/api/presets`, ссылаясь комментарием на файл бэка, которого уже
      нет (`internal/api/handler.go` удалён вместе с REST — `packages/skin/src/presets/client.ts`
      уже целиком на GraphQL). Дописывание пути заменено на `/graphql`
      (`backend/presets/cmd/presets/main.go`'s `mux.Handle("POST /graphql", ...)`), сама логика
      суффиксации (переопределение полным адресом не трогаем) не менялась. Три потребителя
      (`componentInfo`, `content.ts`, `theme-switch`) передают `PRESETS_URL` дальше не читая —
      правки в них не потребовалось. `apps/skin/.mcp` — свой REST-клиент с той же мёртвой ссылкой,
      отдельный пакет, отдельное ТЗ (не трогал).

  - id: fsd-index-barrels
    type: plan
    date: 2026-09-05
    summary: >
      Переезд widgets/entities на структуру, где каждая папка — модуль с ОДНИМ публичным входом.
      Файлы вида `папка/папка.tsx` (header, tree, preview, input, renderer, slot, app)
      переименованы в `папка/index.tsx`. Агрегирующие папки без единственного файла (model, ui,
      entities/component, entities/showcase, widgets/component) получили тонкий index.ts-barrel,
      реэкспортирующий только используемое снаружи. Снаружи теперь ходят в корень слайса
      (#/entities/component), не глубже (не #/entities/component/model/store).

  - id: extensions-and-hash-imports-finding
    type: plan
    date: 2026-09-05
    summary: >
      Расширения убраны из всех импортов (относительных и алиаса #/*). При проверке обнаружено:
      moduleResolution "bundler" даёт мягкое резолвление (без расширения, с авто-index папки)
      только относительным путям и tsconfig.paths — package.json#imports остаётся строгим по
      Node ESM независимо от bundler-режима (проверено эмпирически тремя прогонами tsc с одним и
      тем же импортом: без расширения — падает, с явным /index без расширения — падает, с явным
      /index.tsx — резолвится). Починено зеркальным compilerOptions.paths в apps/skin/tsconfig.json,
      общий @web-core/build/tsconfig не тронут.

  - id: fake-generator-v1-schema-and-hints
    type: plan
    date: 2026-09-06
    summary: >
      Первая версия generateFakeData принимала имя компонента и сама тянула io-схему — по
      результату разговора переделано: генератор принимает схему напрямую, компонент — только
      ключ подсказок. zocker остаётся единственным и общим движком (зод-теги — email/телефон/имя
      и т.д. читает сам). Для полей без зод-тегов (обычный z.string(), например label кнопки, для
      которого нужна конкретная длина, а не случайный текст любой длины) заведена точечная таблица
      подсказок (hints.ts) — применяется через supply() по ссылке на схему конкретного поля, не
      override() по типу, чтобы не задеть остальные строковые поля. Пока одна запись — button.label
      = 10, тест самой механики, не полный охват кита. Подсказки на количество для полей-массивов
      (меню, аккордеон) — тем же приёмом, но отдельным заходом (см. категорию "Не сделано").

  - id: readme-app-template-first-pilot
    type: plan
    date: 2026-09-06
    summary: >
      apps/skin — первое приложение, заполнившее общий шаблон README ("Главное → Анатомия →
      Использование → Настройки → Состояния → IO → Сборки → Рецепт"), заведённый user поверх
      шаблонов "component" (packages/ui) и "engine" (packages/store, первый пилот). genus "app" —
      новый, устоявшейся группы/таксономии для apps/* ещё не было — выбрано "витрина" по смыслу
      этого конкретного приложения, не факт, что подойдёт будущим apps/* без вопроса. Наблюдения по
      адаптации шаблона: "Анатомия" у приложения без импортируемой поверхности — за "адрес" части
      взят URL маршрута, а не импорт-спецификатор (как у движка) и не CSS-селектор (как у
      компонента); "Использование" показывает команды/адреса вместо кода-потребителя — приложение
      никто не импортирует; "Сборки" честно помечены как проверенные только вручную — тестов пока
      нет вообще (см. категорию "Не сделано").

  - id: embed-routes-pathless-layout
    type: plan
    date: 2026-09-08
    summary: >
      Добавлен `/embed/$component/$assembly` для MCP-браузера агента — без хрома витрины. Корень
      дерева маршрутов (`__root.tsx`) раньше монтировал `WorkspaceLayout` безусловно вокруг
      `Outlet`, поэтому любой дочерний маршрут наследовал хром — потребовался pathless layout
      (`_workspace.tsx`, `createFileRoute("/_workspace")`, приём документирован в README
      `@web-core/router` как `_auth.tsx`), в который переехал `WorkspaceLayout`, а шесть прежних
      файлов маршрутов переименованы с префиксом `_workspace.` (URL не изменились, `routeTree.gen.ts`
      подтверждает — `/embed/...` висит сиротой прямо под корнем). Источник данных — новая
      `getContentByName` (`entities/component/model/content.ts`), тонкая обёртка над уже
      существующим `PresetsClient.get`, ранее не вызывавшимся с фронта (только `list`+фильтр).
      Находка по пути: уже запущенный (до этой сессии) dev-сервер закэшировал битую промежуточную
      версию `__root.tsx` через `autoCodeSplitting` роутер-плагина (парсер падал на файле, который
      при чтении с диска был синтаксически корректен) — вылечено полным рестартом dev-сервера с
      очисткой `node_modules/.vite`, не правкой кода.

  - id: chat-ui-split-and-content-trim
    type: plan
    date: 2026-09-08
    summary: >
      `entities/chat/ui/chat` разбит на `control/` (форма — инпут+кнопка одним файлом) и `content/`
      (`item.tsx` — одно сообщение, `container.tsx` — скролл+`For`), `chat/index.tsx` стал тонкой
      сборкой. По ходу решением user из `ChatItem` убраны имя автора и время — в бабле остался
      только текст, аватар держит выравнивание своё/чужое.

  - id: zocker-removed-input-content-select
    type: plan
    date: 2026-09-08
    summary: >
      Зодер выпилен из витрины целиком по прямому решению user ("данные будем юзать только из
      наших пресетов"): `entities/component/utils/fake-generator/`, неиспользуемый
      `shared/utils/zocker/`, зависимость в `package.json`/лок-файле, кнопка "GENERATE FAKE DATA" и
      эффект-автогенерация в `Input`, fallback на `generate()` в `pages/embed/index.tsx` — всё
      удалено. `apps/skin/.mcp` зодер не трогали (отдельный пакет, отдельная зависимость).
      Следом найден и починен баг: `Select` в `Input` был неконтролируемым и не реагировал на
      смену компонента (подпись на триггере залипала от предыдущего) — заведён `value`+сигнал
      `selected`, эффект на `items()` сам ставит пункт `0` по умолчанию или сбрасывает выбор и шлёт
      мок-строку `"-мок-"`, если сохранённых записей для компонента нет.

  - id: chat-real-session-api
    type: plan
    date: 2026-09-09
    summary: >
      Чат подключён к реальной сессии агента вместо локального мока. Контракт от user: `POST
      /sessions/{id}/messages` (`Authorization: Bearer <общий токен>`, `X-User-Login: <логин>`,
      тело `{text, context: {component, variant, outfit}}`), ответ — один синхронный JSON
      (`event: "run-finished"`, `reply`/`refusal`/`did`), без стрима/поллинга. Уточнено с user перед
      реализацией: session id = имя ТЕКУЩЕГО компонента (агент хранит чат per-session, сессия
      должна быть в рамках компонента); токен — билд-тайм env `VITE_CHAT_TOKEN`, тем же приёмом,
      что `VITE_PRESETS_URL`; `variant`/`outfit` в `context` пока не шлём — их нет ни в роутинге,
      ни в каком-либо сторе фронта; базовый адрес — `VITE_CHAT_URL`, дефолт
      `https://150.251.145.87/api` (назван user, отдельный сервис, не служба пресетов).

      Новый `entities/chat/model/api.ts`'s `sendChatMessage`; `store.ts`'s `sendMessage` стал
      асинхронным (оптимистичная запись сообщения юзера → ожидание → дозапись `reply`/`refusal`
      единым путём → ошибка сети тоже текстом в чат, не проглатывается) и завёл сигнал `pending`,
      которым `ChatControl` дизейблит форму на время запроса. Моковая затравка сообщений
      (`MOCK_MESSAGES`) убрана — история пуста до первой реальной переписки.

      Не сделано осознанно (нет источника/эндпоинта на момент разговора): `variant`/`outfit` в
      context; загрузка истории чата при заходе на компонент (нет GET-эндпоинта); рендер `did`
      (вызовов MCP-тулов из ответа) в UI — виден только `reply`/`refusal`.

  - id: slot-loader-around-renderer
    type: plan
    date: 2026-09-09
    summary: >
      Точечное ТЗ user: "тут .../slot/index.tsx надо сделать лоадер, пока компонент не загрузился.
      лоадер должен быть только для Renderer а не для всего слота". Граница ожидания положена
      ровно вокруг `Renderer` внутри `CarouselItem` (`Show`+`fallback`), всё остальное в слоте
      остаётся на экране. Слот оставлен глупым — признак приходит пропом `loading`, считает его
      вызывающий; в `ComponentHandle` заведён `loading()` (`componentInfoAtom.status === "pending"`),
      потому что `!ready()` тут не работает: ресурс держит последнее известное `info` и в pending,
      значит `ready()` на смене компонента остаётся true при заведомо чужом info.
      Спиннера не рисовали: своей графики у витрины нет по решению user 2026-08-27 (`index.html`),
      а в ките такого компонента нет — ожидание сказано словом через `Typography`.
      По ходу найдено (не чинено, не моё ТЗ): уже запущенный dev-сервер на 5174 отдавал старую
      версию модулей — тот же кэш трансформа, что и в `embed-routes-pathless-layout`; проверка
      велась на своём сервере (5175, `--force`), чужой процесс не трогали.

  - id: chat-session-and-sse
    type: plan
    date: 2026-09-09
    summary: >
      ТЗ бокса от user («как разговаривать с боксом») переписало транспорт чата, а следом сам user
      назвал, где сессии жить: «после логина сразу отлетает запрос и получаем айди сессии, ложим его
      рядом с логином юзера; дальше из любого места юзер может взять свой айди сессии или логин; в
      чате подставляется логин и айди сессии из стора юзера». Сделано так: `login()` заводит сессию
      (`POST /api/sessions`) и кладёт выданный id рядом с логином (`currentSession` + ключ `session`
      в localStorage), `logout()` чистит оба, `renewSession()` заводит заново. Чат сессию не
      заводит и не хранит — только подставляет; на 404 просит стор обновить её и повторяет ход раз.
      Транспорт (адрес, токен, заголовки, `createSession`) вынесен в `shared/api/box`, чтобы
      `entities/user` и `entities/chat` не импортировали друг друга ради него.

      Прошлая механика снята целиком: session id больше не имя компонента и не заводится первым
      сообщением; ответа агента в теле POST нет — приходит квитанция с `run`, а сам ответ читается
      событием `run-finished` из SSE-потока `GET /sessions/{id}/events` (заголовки — значит
      `EventSource` не годится), поток открывается ДО POST. Обрыв потока не теряет прогон:
      дочитывание через `/runs` + `/messages` + `/runs/{run}/steps`.

      Проверено живьём против бокса (2026-09-09, `https://150.251.145.87/api`, токен от user в
      `apps/skin/.env.local`, в гит не едет): вход → 201 и id в сторе; сообщение → 202-квитанция;
      ответ агента приехал `run-finished`'ом из потока и осел в чате. По ходу проверки найдено, что
      дев-сервер не видел правок вовсе — репозиторий подмонтирован с хоста (fstype `9p`), а на нём
      inotify не отдаёт событий; лечится переносом репо в контейнерный том (`Clone Repository in
      Container Volume`, как и написано в `.devcontainer/devcontainer.json`), решение за user.

      Найдено тестом и починено в коде, а не в тесте: логин кириллицей нельзя положить в
      `X-User-Login` — HTTP-заголовок это байты, и `fetch` роняет запрос до сети. Не-ASCII логин
      едет процентно закодированным, латиница как есть.

      Открытым остаётся (решение не наше): бокс увидит закодированный логин как имя личности — нужен
      читаемый, логины надо ограничивать латиницей на форме входа; пароль в `entities/user/ui/auth`
      по-прежнему захардкожен на клиенте и никак не связан с `VITE_CHAT_TOKEN`; сессия теперь одна
      на юзера, а не на компонент — в одной памяти агента идут разговоры про все компоненты
      (компонент едет в `context` каждого сообщения), и с ростом истории каждый ход дорожает.
