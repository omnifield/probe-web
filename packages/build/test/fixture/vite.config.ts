// Ровно то, что ляжет у потребителя: импорт, вызов, `export default`. Ни про solid-плагин,
// ни про версию Vite тут не знают — всё за точкой `/vite` (PROBEWEB-4).
import { defineConfig } from "@omnifield/probe-web-build/vite";

export default defineConfig();
