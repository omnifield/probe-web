// ПОДПУТЬ `./flat` — плоская форма вывода и ТОЛЬКО она.
//
// Вход узкий намеренно. За разворачиванием стоит postcss со спутниками, и он попадает в сборку
// потребителя от одного импорта; собери мы здесь заодно модель и порождение — «postcss только
// там, где нужен» держалось бы на том, что потребитель импортирует правильное имя из широкого
// входа, а это не механизм.
//
// Кому нужны обе формы, берёт обе: порождение из `.`, разворот отсюда.

export { flattenCss } from "./flatten.js";
