// СВЯЗКА С ИСТОЧНИКОМ ПАСПОРТОВ (`PWEB-94`) — источник называется ОДИН раз и приезжает связкой:
// проверить наряд одним источником, а породить другим, не выражается. Источник — `passportOf`
// ЭТОГО кита: каждый рецепт-доказательство (`src/*/*.recipe.ts`) одевает СВОЙ же паспорт, тем же
// вызовом, каким его одел бы настоящий потребитель.
//
// Общее на несколько компонентов — сюда, в `test/`, а не в файл одного из них: решение того же
// рода, что и `test/dom.jsx`.

// Корневой вход, НЕ `/model`: у `/model` своя связка (`bound.ts`) без печати — `generateSkinCss`
// несёт только связка корня (`generate.ts`, «модель плюс печать»). Имя одно на обе НАМЕРЕННО
// (README пакета `skin`), и здесь нужна печатающая половина.
import { withPassports } from "@omnifield/probe-web-skin";

import { passportOf } from "../src/passport.js";

export const { assemble, generateSkinCss } = withPassports(passportOf);
