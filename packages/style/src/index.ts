// КОРНЕВОЙ ВХОД набора значений. Зависимостей на `ui`, `runtime` и `build` тут нет и быть не
// может: направление зависимостей одностороннее (`kb:PROBEWEB-4`).
//
// РЕАКТИВНОЙ ЧАСТИ БОЛЬШЕ НЕТ (`PWEB-56`). Здесь жили `registerTheme` — реестр тем — и
// `createThemeController` — сигнал текущей темы и режима. Сняты не потому, что не работали, а
// потому что у них не осталось ПРЕДМЕТА: тем не существует. Пресетное поколение кончилось,
// единицей стал скин, режим стал его половиной (`PWEB-50`), надеваемая палитра снята оттуда же.
//
// Признак у этого класса правок один и тот же, и он повторился третий раз подряд: работает,
// потребителей нет, объяснение начинается со слова «раньше».
//
// СЛЕДСТВИЕ, названное вслух: `solid-js` зоне больше не нужен ВООБЩЕ. Его трогал ровно один
// файл, и этот файл — ушедший. Одноранговая зависимость снята из манифеста; проверено запуском
// в чистой установке, а не чтением (`test/pack.test.ts`).
//
// ОТСЮДА ЖЕ — ОТКРЫТЫЙ ВОПРОС ПРО УЗКИЙ ПОДПУТЬ. `/values` заводился как вход БЕЗ
// реактивности (`PWEB-44`); реактивности не стало, и корень с узким входом теперь совпадают
// имя в имя. Второй двери к одному и тому же зона не держит нигде — это ровно тот «второй
// источник правды», против которого написано остальное. Но подпуть уже потребляют
// (`packages/skin`), поэтому снятие — согласованное ломающее, а не попутная правка: сначала
// потребитель возвращается на корень, потом уходит дверь. Заявка поднята.
//
// ИНСТРУМЕНТОВ СТИЛИЗАЦИИ ЗДЕСЬ НЕТ (`PWEB-3`). `cn`, `createStyle` и реэкспорт `cva`
// уехали в самостоятельную необязательную поставку `@omnifield/probe-web-style-tools`.
//
// CSS через этот вход НЕ идёт: манифест объявляет `sideEffects: false`, и импорт-побочка
// из корня была бы выброшена tree-shaking'ом. Стили едут подпутём `/base.css`.

export * from "./values.js";
