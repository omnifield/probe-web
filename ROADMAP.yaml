# Дорожная карта репозитория целиком — формат тот же, что у роадмапов пакетов и компонентов (см.
# packages/ui/ROADMAP.yaml, шапка). Единственный источник правды: статус проставляется рядом с
# работой, в момент когда она сделана.
#
# ОТЛИЧИЕ от роадмапов пакетов/компонентов: репозиторий как целое почти не ведёт СВОИХ задач —
# работа планируется на уровне зоны (у каждой из них свой ROADMAP.yaml: packages/ui,
# products/windshift и т.д., см. `.claude/harness.yaml` за списком зон). Здесь остаётся то, что
# зоне не принадлежит целиком — межзонные инициативы и решения по репозиторию как единице.
#
# Список пуст сознательно: наполняется по мере появления настоящих межзонных задач, не заранее.

categories: []
