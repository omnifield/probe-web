// КАРКАС ПРИЛОЖЕНИЯ — пока честно пустой, не заглушка под что-то большее.
//
// Зона только что заведена (`PWEB-155`): дев-сервер и сборка работают, а витрины диаграмм ещё
// нет — рисовать её раньше первого настоящего компонента-диаграммы нечем. Когда появится, каркас
// соберётся тем же приёмом, что у `products/skin/src/app/shell.ts` (RenderTree из
// `PassportAssembly`, не голая вёрстка) — это не сделано здесь заранее, потому что заводить
// механику раньше, чем она реально понадобится, здесь не практика (см. `apps/panel/README.md`:
// «не заводить механику заранее»).

export function App() {
  return <p>Diagrams — витрина ещё не собрана.</p>;
}
