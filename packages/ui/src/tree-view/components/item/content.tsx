export function TreeItemContent(props) {
  traceLife("ui.tree-item-control");

  return (
    <TreeView.BranchContent>
      сюда будет подставляться то что заданно в схеме контейнер нужен для
      механик арка
    </TreeView.BranchContent>
  );
}
