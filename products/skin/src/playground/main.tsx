// Точка входа стенда. Порядок тот же, что у соседей и у скелета потребителя: базовый CSS,
// тема, оформление СТЕНДА, затем `mount` из зоны `runtime`.
//
// Чего здесь намеренно НЕТ — импорта нашего оформления кита (`skin.css`). Стенд ставит и снимает
// его на живую (`knobs.ts`), потому что показать надо именно это: оформление подключается отдельно
// и снимается отдельно, кит без него остаётся голым (kb:PROBEWEB-11, правило первое).
//
// Ставится оно ВМЕСТЕ С ПРЕСЕТОМ и снимается вместе с ним (решение user 2026-08-18): своей ручки у
// оформления нет. Пресетов нет — здесь остаётся голый кит, и это честное состояние, а не поломка.
import "@omnifield/probe-web-style/base.css";
import "@omnifield/probe-web-style/themes.css";
import "./playground.css";

import { mount } from "@omnifield/probe-web-runtime";

import { App } from "./app.jsx";

mount(() => <App />);
