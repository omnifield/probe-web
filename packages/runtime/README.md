# @web-core/runtime

Рантайм web-core: **`mount()`, контракт `#root` и механика подключения скина** — то, что
зовёт скелет приложения у потребителя. Первая из четырёх замороженных точек выпуска (контракт
зон — `PROBEWEB-4`).

Пакет не знает ни про сборку, ни про стили, ни про компоненты: build-инструментов в его
зависимостях нет и быть не может — ради этого зона и отделена от `build`.

## Поверхность

```ts
import {
  mount,               // поднять приложение в #root
  makeSkinSwitch,      // надеть скин и половину, снять, восстановить запомненное
  createSkinConnection, // то же самое, но worn — Solid-сигнал, а не функция по запросу
  checkStyleOrder,     // приехал ли базовый CSS под надетым скином
} from "@web-core/runtime";
```

Подпуть у пакета **один**: механика едет именованными экспортами той же точки, отдельного
пакета и отдельного подпути не заводим (`PROBEWEB-13`). Всё остальное внутри — внутреннее
и ломается свободно.

**Заморожены две вещи — сигнатура `mount()` и идентификатор `#root`.** `main.tsx` едет к
потребителю классом `placed-once`: положен один раз и не обновляется никогда, поэтому вызовы
в нём живут вечно ровно в той форме, в какой их туда положили. Запрета на соседние экспорты
из этого не следует, но и они, оказавшись в скелете, замерзают вместе с ним — состав
поверхности меняет architect, а не зона.

## Контракт `#root`

Точку монтирования рантайм ищет **сам** — элемент с `id="root"` в документе:

```html
<div id="root"></div>
```

Идентификатора нет в сигнатуре намеренно: передай его аргументом — и заодно с вызовом
заморозится способ разметки страницы, а он обязан двигаться. Элемента нет — `mount()`
**падает с внятным сообщением**, а не рисует молча в пустоту:

```
[web-core-runtime] mount(): в документе нет элемента #root.
Точку монтирования рантайм ищет сам — добавь в index.html <div id="root"></div>.
```

`#root` — часть замороженной поверхности наравне с самим экспортом. Менять его нельзя без
мажора и разговора с architect.

## Владение и очистка

`render()` из `solid-js/web` создаёт владеющий контекст (`createRoot`) и возвращает `dispose`.
Дока Solid прямо предупреждает: **без явного `dispose` вычисления не управляются и текут**
(фонд `solidJS/sources/solid-docs-ownership.md`).

Что решено и почему:

- **Наружу `dispose` не отдаётся.** Тип точки заморожен как `void`; вернуть ручку — значит
  изменить замороженную поверхность, а это разговор с architect, не решение внутри зоны.
- **Но и не выбрасывается.** Ручку держит сама зона — `WeakMap` «узел-хозяин → `dispose`».
  Модульная переменная тут не годится: она пережила бы сам узел и вечно держала бы замыкание
  над оторванным от документа деревом. Умер ключ-узел — умерла запись.
- **Повторный вызов на том же `#root`** (HMR, перезапуск) сначала **уничтожает** прежний
  корень и только потом монтирует новый. Инвариант зоны — «один документ, одно приложение»:
  `#root` в контракте один, и складывать в него корни стопкой значит держать живыми
  вычисления, до которых уже никто не дотянется.
- **Страница закрывается — уничтожать нечего:** корень умирает вместе с документом, и
  вешать ради этого `pagehide` было бы обрядом, а не очисткой.

Понадобится потребителю управляемый `dispose` (микрофронтенды, несколько приложений на
странице) — это заявка на изменение контракта, и адресат её architect.

## Одна механика на один поток

Контракт — `PROBEWEB-13`, инварианты — `SKIN-7`, шов — `SKIN-5`.

**Надевание скина — единственная механика зоны.** Второго пути к тому же результату нет и не
заводится: два ответа на вопрос «во что одета страница» расходятся молча, а сходятся только в
чьей-нибудь пробе, зовущей обе двери. Пресетный путь (`applySkin` / `restoreSkin` / `readSkin`,
`data-theme` на корне) снят целиком — не оставлено ни обёртки, ни «совместимости».

**Механика — удобство, а не путь к результату.** Страница, подключившая файл скина сама и
поставившая `data-skin`, выглядит так же. Перестало быть верным — механика вышла за границу.

Отсюда же: приложение **без единого скина** запускается и пригодно к работе (инвариант 4). Мы
не делаем оформление обязательным.

### Две вещи механика НЕ знает — их ей сообщают

Это не неудобство, а несущее свойство: и перечень скинов, и маркерная пара принадлежат чужим
зонам и меняются без нашего ведома.

| что | кому принадлежит | почему не зашито у нас |
|---|---|---|
| перечень скинов | приложение и его поставщик | база не может зависеть от `products/` (`PROBEWEB-4`): временная зона, обязанная уехать, оказалась бы вшита в фундамент |
| маркерная пара базы | `style` | литерал стал бы тихой связью — маркер уже переезжал, и литерал пережил бы переезд молча, начав врать |

Побочная выгода обоих решений одна: **чужой поставщик скинов и чужая база встают в эту
механику без единой правки.**

### Проверка порядка подключения

Скин без базового CSS даёт вид, где производные посчитаны от умолчаний. Выглядит это как
«скин почти работает» — **хуже явной поломки**, потому что чинить такое никто не идёт.

**Маркер — ПАРА: свойство и значение**, а не имя.

```ts
import { BASE_MARKER } from "@web-core/style";   // { property, value }

checkStyleOrder({ marker: BASE_MARKER });
// [web-core-runtime] порядок подключения нарушен: скин «twitter» надет, а базового CSS
// нет — на корне box-sizing обязан быть «border-box», а он равен «content-box». …
```

Прежде принималось имя кастом-свойства, а приезд предъявлялся тем, что свойство **объявлено**.
Держалось это на форме имени: `--` отличало наше от браузерного, потому что кастом-свойств
браузер не объявляет. Основание ушло вместе с поставщиком — базовый слой стал одним сбросом,
кастом-свойств в нём не осталось, и маркером стало обычное свойство. А обычное объявлено
**всегда**: «свойство нашлось» истинно и без нашего листа, то есть проверка врала бы зелёным —
худший из отказов. Значение отличает надёжнее любой формы имени.

Отсюда и отказ: **пустая половина пары — исключение**. Свойство без ожидаемого значения это не
неполнота, а тихо соврамший гейт. Требования «имя обязано начинаться с `--`» больше нет: оно
ушло вместе со своим основанием.

| статус | что значит | шумит ли |
|---|---|---|
| `ok` | свойство на корне приняло ожидаемое значение — база приехала | нет |
| `missing-base` | скин надет, а значение чужое — **порядок нарушен** | `console.error` |
| `no-skin` | ни базы, ни скина — голый кит, заявленное свойство (инвариант 4) | нет |

Разбор несёт `seen` — что на корне **нашлось**. По нему видно разницу между «свойства нет
вовсе» и «свойство занято чужим значением», а это разные поломки.

**Хотя бы одна проба зоны ходит настоящей парой** — той самой, что получит скелет
(`@web-core/style` в пробных зависимостях; в поставку он не едет). Пока все пробы
ходили собственным маркером, переезд настоящего прошёл мимо них: они остались зелёными, а
вызов в скелете сломался.

**Что проверка видит и чего не видит.** Она отвечает на «база приехала или нет», а не на «в
каком порядке склеены листы»: настоящий порядок правил в каскаде из DOM не читается. Ловится
следствие, ради которого правило и заведено, — молча пропавшая база под надетым скином.

**Не бросает и не останавливает приложение.** Механика — удобство, и диагностика вида не имеет
права ронять работающее приложение потребителя: она кричит в `console.error` и отдаёт разбор
значением. Что с ним делать — упасть в dev, показать плашку, промолчать — решает приложение.

Пустая половина пары — **отказ сразу**: свойство без ожидаемого значения врёт зелёным, потому
что обычное свойство объявлено всегда.

## Надевание и снятие скина

**Скин — вид целиком: переменные и правила вида, одной единицей.** Приложение обязано уметь
четыре вещи: назвать источник скинов, надеть нужный по имени, снять его и не потерять выбор
между заходами. Код этого живёт здесь, а не в зоне скина: её поставка — CSS, ни одного модуля.

```ts
const skin = makeSkinSwitch(source, { fallback: { skin: "twitter", mode: "dark" } });

await skin.restore();                        // запомненные скин и половина — или ничего
await skin.names();                          // перечень источника, как есть
await skin.wear("brutal", { mode: "dark" }); // надеть скин и половину, запомнив обоих
skin.worn();                                 // { name: "brutal", mode: "dark" }
skin.takeOff();                              // снять — остаётся голый кит
```

**Половина приезжает вместе со скином, и другой двери к ней нет.** Она часть скина, а не
свойство документа: отдельная ручка была бы вторым ответом на вопрос «во что одета страница».
Названа — ставится; не названа — встаёт запомненная; не запомнено ничего — не ставится ничего,
свою половину назовёт сам скин.

### Источник принимается, а не зашивается

```ts
const source = {
  names: () => ["twitter", "brutal"],   // можно и обещанием
  css: (name) => fetchSkinCss(name),    // ТЕКСТ стилей, а не адрес файла
};
```

Ни про зону скина, ни про адрес хранилища механика не знает **никогда**: направление
зависимостей не разворачивается, и чужой поставщик скинов встаёт сюда без единой правки.

**Валюта — текст стилей, а не адрес файла.** Так устроен реальный путь скина: хранилище отдаёт
его структурой (переменные и рецепты), а CSS из неё порождает генератор прямо в браузере —
файла с адресом на этом пути не существует ни на одном шаге. У кого файл есть, тот отдаёт его
текст сам: как его достать, знает он, а не мы.

Перечень нужен механике ровно для одного: **запомненное имя, которого в источнике больше нет**
(скин удалили, поставщика сменили), надевать нельзя. Название для человека, автор, содержимое —
это интерфейсу выбора, и он берёт их у источника напрямую, минуя нас.

### Что механика ставит на корень

Надетый скин опознаётся атрибутом `data-skin` на `<html>`:

```html
<html data-skin="brutal">
```

**Файл скина цепляться за этот атрибут не обязан** и по умолчанию не цепляется: скин подключён
ровно тогда, когда его лист стоит в документе. Атрибут отвечает на вопрос «что надето» —
приложению, редактору и человеку в инструментах разработчика.

Соседний `data-theme` — про другой предмет (набор значений) и снимается вместе с ним, а не
переиспользуется под скин.

### Лист — обычный тег, и это решение

Сверено с рынком **2026-08-20**: конструируемые листы (`CSSStyleSheet` +
`document.adoptedStyleSheets`) есть во всех движках с марта 2023 и считаются сегодня основным
способом отдать документу стили строкой. **Не берём** по проверяемой причине: принятые листы
стоят в каскаде ПОСЛЕ всех листов документа и перебивают их всегда, а от механики требуется
обратное — страница, подключившая файл скина сама, обязана выглядеть **так же**. У такой
страницы скин это обычный лист в голове документа, и совпасть с ним может только обычный лист.
Побочно тег виден в инструментах разработчика — человек может проверить, что на нём надето.

Смена скина — **замена текста в том же листе**, а не «снять один тег, повесить другой»: между
снятием старого и разбором нового браузер успевает нарисовать кадр голого кита, и переключение
мигает. Стережётся пробой на тождество узла.

### Память выбора — предмет, а не путь

**Человек, выбравший скин и половину, в следующий заход видит их же.** Это не удобство сбоку, а
пункт потока, и он проверяется пробой, а не подразумевается.

Запись **идёт слиянием**: скин и половину выбирают порознь — надел скин, не тронув половину, — и
запись целиком стирала бы ту половину выбора, которую в этот раз не называли.

`skin: null` в записи — это **снятый скин**, а не «про скин не записано». Разница несущая:
восстановление уважает снятое и запасное приложения взамен него не надевает, иначе снять скин
между заходами было бы невозможно — он воскресал бы при каждом запуске.

`fallback` — явный выбор потребителя, и другого источника у выбора нет; половина называется в
нём отдельно от скина, потому что и выбирают их отдельно. Не названо — остаётся голый кит: не
вспомнилось и запасного не назвали, значит нечего.

### Чего механика не делает

- **Ничего на заведении и на импорте** — ни обращения к источнику, ни узла в документе, ни
  чтения хранилища. Лист заводится при первом надевании, и до него приложение неотличимо от
  приложения без механики.
- **Не трогает дерево.** Вид меняется листом и атрибутом, поэтому открытые панели, фокус и
  ввод переживают смену скина; `mount()` при ней не зовётся.
- **Половину приносит и уносит.** Надевание ставит названную, а без имени — запомненную;
  снятие уносит её вместе со скином. На голой странице половины нет вовсе: она половина скина,
  а скина нет — половине не от чего быть.
- **Не глотает отказ источника.** Приложение позвало надевание явно — оно имеет право узнать
  отказ; полусостояния при этом не остаётся, на корне остаётся прежнее.
- **Не даёт приехать протухшему ответу.** Человек в редакторе щёлкает скины быстрее, чем они
  приезжают: побеждает выбранный последним, а не приехавший последним.

**Механика необязательна.** Приложение, подключившее файл скина само и поставившее `data-skin`,
выглядит так же — это гейт зоны, а не пожелание. И **без скина приложение работает**: голый
кит с адресными атрибутами — законное состояние продукта, а не отказ.

### Solid-примитив: `createSkinConnection`

`SkinSwitch.worn()` — обычная функция, читает атрибут по запросу и сама за собой не следит.
Solid-продукту, которому нужно показать «во что одета страница» живьём, `createSkinConnection`
отдаёт то же самое, что `makeSkinSwitch`, но `worn` — сигнал:

```tsx
import { createSkinConnection } from "@web-core/runtime";

function App() {
  const skin = createSkinConnection(source, { fallback: { skin: "brutal", mode: "light" } });

  onMount(() => void skin.restore()); // boot по-прежнему зовёт продукт — сигнал досинхронизировать не нужно

  return (
    <button onClick={() => skin.setMode(skin.worn()?.mode === "dark" ? "light" : "dark")}>
      {skin.worn()?.mode ?? "без скина"}
    </button>
  );
}
```

- `worn` обновляется сам после `wear()`/`takeOff()`/`restore()` — досинхронизировать руками
  нечего;
- `setMode(mode)` — сменить половину надетого, не таская наружу текущее имя скина;
- `dispose()` наружу не отдаётся: примитив сам вешает его на `onCleanup()`. **Зовите
  `createSkinConnection` внутри компонента или `createRoot()`** — без владельца Solid `onCleanup`
  не запомнит, кого звать при размонтировании.

**Чего примитив не решает:** горячую замену модуля потребителя. `import.meta.hot.dispose()`
всегда про ТЕКУЩИЙ модуль — библиотека не может повесить его за модуль, который её зовёт.
`onCleanup()` закрывает уборку по владению Solid; закрывает ли этого достаточно для конкретного
HMR-сценария продукта — проверяется в нём самом, руками.

## Perf-трейсы

Молчат по умолчанию, включаются явным глобальным флагом:

```js
globalThis.__WEB_CORE_RUNTIME_TRACE__ = true;
// [web-core-runtime] mount — 1.83ms
```

Автоопределения по dev-режиму сборщика нет намеренно: пакет приезжает потребителю
зависимостью, а подстановку в файлы из `node_modules` Vite не делает — условие осталось бы
вечно ложным, то есть трейсы были бы мёртвым кодом.

## Установка

`solid-js` — **peer-зависимость**: две копии Solid в дереве ломают реактивность.

```
pnpm add @web-core/runtime solid-js
```

ESM-only, `"type": "module"`, `sideEffects: false`, в тарбол едет только `dist` и эта дока.

## Раскладка

```
packages/runtime/
├── src/
│   ├── index.ts            # ВСЯ поверхность наружу — и mount(), и механика скина
│   ├── skin-root.ts        # корень документа как среда, ВНУТРЕННЕЕ
│   ├── skin-memory.ts      # память выбора, ВНУТРЕННЕЕ
│   ├── skin-sheet.ts       # лист стилей надетого скина, ВНУТРЕННЕЕ
│   └── trace.ts            # perf-трейсы, ВНУТРЕННЕЕ (в exports не объявлено)
├── test/
│   ├── mount.test.tsx      # приложение поднимается в DOM (jsdom + JSX-трансформ)
│   ├── skin-switch.test.ts # надевание, половина, снятие (jsdom БЕЗ Solid — несущее)
│   ├── style-order.test.ts # проверка порядка подключения, там же
│   └── surface.test.ts     # поверхность и тарбол — перечнем, а не на глаз
├── tsconfig.json           # конфиг самого пакета (базу потребителю отдаёт зона build)
├── tsconfig.build.json     # эмит dist по src
└── vitest.config.ts        # три проекта: mount, skin, surface
```

Публичная поверхность объявлена ЦЕЛИКОМ в `src/index.ts`, а не собрана реэкспортом из
соседей: реэкспорт-бочка в `dist/index.d.ts` запрещена и стережётся пробой. Соседние модули
внутренние и в `exports` манифеста не объявлены.

## Проверки

```
pnpm run typecheck   # tsc --noEmit
pnpm run build       # dist
pnpm test            # сборка + все три проекта vitest
```

Тест зоны поднимает **настоящее** приложение на Solid в JSDOM и утверждает отрендеренный узел
в `#root`, отдельными кейсами — живую реактивность после монтирования, уничтожение прежнего
корня при повторном вызове и внятный отказ без `#root`.

Пробы механики скина стоят **отдельным проектом без Solid и без JSX-трансформа**, и это
несущее решение, а не экономия: механика обязана работать до монтирования и вне Solid, а
проба, поднимающая Solid, доказывала бы обратное тому, что мы утверждаем. Проверка порядка
подключения ставится настоящими листами стилей — она спрашивает ВЫЧИСЛЕННЫЙ на корне токен, и
подставленное значение проверяло бы саму подстановку.

Официальная `@solidjs/testing-library` здесь сознательно **не** используется (сверено
2026-08-08, фонд `solid-testing-library`): её `render` создаёт свой контейнер, а предмет
проверки ровно обратный — что рантайм находит точку монтирования сам, в документе. JSX-трансформ
и `resolve.conditions: ["development", "browser"]` взяты из доки как предписано.
