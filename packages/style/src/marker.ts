// МАРКЕР ПРИЕЗДА БАЗЫ — то, по чему снаружи проверяют, что базовый CSS подключён.
//
// ЗАЧЕМ ЭТО ЗДЕСЬ. Проверку порядка подключения делает `runtime` (`checkStyleOrder`), но имя
// маркера она принимает АРГУМЕНТОМ и не знает (`kb:PROBEWEB-13`): знание «что обязано приехать
// с базой» принадлежит нашей зоне. Литерал на той стороне стал бы тихой связью — переименуем
// здесь, и проверка там начнёт врать: кричать «базы нет» на исправном приложении либо, что
// хуже, молчать на сломанном. Ни сборка, ни пробы этого не покажут.
//
// Цена ошибки несимметрична: имя уезжает в скелет, а скелет кладётся потребителю классом
// `placed-once` и НЕ ОБНОВЛЯЕТСЯ НИКОГДА. Литерал, попавший туда, чинится только правкой
// руками в каждом уже созданном продукте.
//
// ─────────────────────────────────────────────────────────────────────────────────────────
// МАРКЕР ПЕРЕЕХАЛ НА СБРОС (`PWEB-66`), и переехал вынужденно.
//
// Был `--control-target-min` — пол нормы (WCAG 2.2, 2.5.8): не ступень и не вкус, палитра,
// объявившая его, дефект по определению. Свойство хорошее, но носителя не стало: в листе не
// осталось НИ ОДНОГО кастом-свойства, а значит и токена, которым можно предъявить приезд.
//
// Носителем стал сам сброс. Он и есть теперь весь базовый слой, то есть отвечает ровно на тот
// вопрос, ради которого маркер заведён: приехал лист или нет. `box-sizing` взят потому, что
// его значение в сбросе (`border-box`) ОТЛИЧАЕТСЯ от умолчания браузера (`content-box`), —
// по нему видно не только «свойство есть», но и «приехало именно наше».
//
// ЧТО ЭТО ЛОМАЕТ, названо прямо, а не оставлено на потом. Маркер перестал быть
// кастом-свойством, а `runtime.checkStyleOrder()` принимает ТОЛЬКО кастом-свойство: имя без
// двух дефисов она отвергает исключением, а нашла бы — читала бы `getComputedStyle` и на
// `box-sizing` получала бы непустое значение ВСЕГДА, в том числе без нашего листа. То есть
// прежняя форма проверки на новом маркере либо падает, либо врёт зелёным.
//
// Это шов между зонами, и чинится он не здесь: `runtime` нужно принимать не имя токена, а
// пару «свойство и ожидаемое значение». Заявка поднята. Пока её нет, маркер продолжает
// экспортироваться и отвечает на свой вопрос ТЕКСТОМ листа — так его и спрашивает эталон.

import { RESET_MARKER } from "./css/written.js";

/**
 * Имя свойства, которым проверяется ПРИЕЗД БАЗЫ.
 *
 * ЧЕМ ОБЯЗАН БЫТЬ МАРКЕР — три свойства, и все три проверяются пробой (`test/marker.test.ts`),
 * а не соблюдаются на память:
 *
 * 1. **Объявлен базовым слоем** — иначе он не отвечает на вопрос, ради которого заведён.
 * 2. **НИКОГДА не объявлен палитрой.** Маркер, уехавший в палитру, стирает разницу между
 *    «база не приехала» и «палитра не выбрана». Сброс палитрой не объявляется по построению:
 *    палитра несёт значения, а не правила.
 * 3. **Значение не зависит ни от палитры, ни от режима** — литерал, без единого `var()`.
 *
 * Имя собирается из самой записи сброса (`src/css/written.ts`), а не переписано строкой:
 * второй копии имени в зоне нет, и правка сброса двигает маркер сама.
 */
export const BASE_MARKER = RESET_MARKER;
