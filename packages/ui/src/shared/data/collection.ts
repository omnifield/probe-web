// ОБЩАЯ коллекция Ark — одна точка на весь кит, не по копии на каждый компонент, у которого она
// есть (`PWEB-195` continuation, 2026-08-30, user: «давай их не в утилиты, а в шаред — это
// логично даже не учитывая автоген»).
//
// `createListCollection`/`CollectionItem`/`ListCollection` не специфичны ни для листа, ни для
// селекта — это общий механизм Ark для любого компонента, построенного на выбираемом перечне
// пунктов (`@zag-js/collection` под капотом, `CollectionItem`/`ListCollection` реэкспортированы
// оттуда же самим Ark). Раньше select и listbox каждый вёз СВОЮ копию этого реэкспорта из своего
// же Ark-подпути (`@ark-ui/solid/select`, `@ark-ui/solid/listbox`) — работало, но задваивало одно
// и то же имя в двух разных модулях. `src/index.ts` держал это руками, разводя по алиасам; при
// переходе на порождение обхода папок (`export *` на каждую) такое задвоение стало бы настоящей
// коллизией имён, а не только лишней строкой. Берём из КОРНЕВОГО входа `@ark-ui/solid` — того же
// самого объекта, не второй копии — а не из подпути конкретного компонента: ровно то место,
// откуда его в итоге получают и `select`, и `listbox`, и получит любой следующий компонент с
// коллекцией.

export { createListCollection, type CollectionItem, type ListCollection } from "@ark-ui/solid";
