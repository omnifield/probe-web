{
  "//": [
    "Своя настройка, а не `@omnifield/probe-web-build/tsconfig` — и это не отступление от",
    "kb:PROBEWEB-5, а прямо записанное там исключение: у серверного процесса базы фронта нет,",
    "брать оттуда нечего, оснастка сборки ему не нужна.",
    "",
    "Проверяем ИСХОДНЫЙ JS с типами в JSDoc (`checkJs`), а не собираем TS в dist: сборки в",
    "этой зоне нет вообще, контейнер запускает ровно те файлы, что лежат в репозитории.",
    "`noEmit` — потому что выхлоп никому не нужен, нужна только проверка."
  ],
  "compilerOptions": {
    "target": "esnext",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "lib": ["esnext"],
    "types": ["node"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "exactOptionalPropertyTypes": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true
  },
  "include": ["src", "test"]
}
