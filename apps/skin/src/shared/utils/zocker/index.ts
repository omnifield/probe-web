// Один вход к `zocker` для всей витрины — импортировать пакет напрямую в каждой сущности вместо
// этого незачем: путь один, и его же поменять, если библиотека когда-нибудь сменится (та же
// причина, что у `packages/ui/src/shared/utils/collection.ts`).
//
// СВОЕЙ ЛОГИКИ ЗДЕСЬ НЕТ — ни сида по умолчанию, ни числа записей. И то, и другое решает
// вызывающий на месте, а не этот файл за него: то же самое, чего просил user, когда снимал
// `EXAMPLES_PER_COMPONENT` из `content.ts` — «сколько хочу, столько и укажу».
export { zocker } from "zocker";
