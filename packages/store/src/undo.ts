// Аддон `@xstate/store`: `createStore({...}).with(undoRedo())` — добавляет `store.trigger.undo()`
// и `store.trigger.redo()`. См. довод по подпутям в `./persist`.
export * from "@xstate/store/undo";
