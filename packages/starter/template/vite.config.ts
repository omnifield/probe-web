// Ни версии Vite, ни solid-плагина, ни дев-сервера здесь нет намеренно: всё это живёт за
// точкой `@omnifield/probe-web-build/vite` и двигается её выпуском (kb:PROBEWEB-4). Файл
// положен один раз и дальше твой — обвес в него больше не заглянет.
import { defineConfig } from "@omnifield/probe-web-build/vite";

export default defineConfig();
