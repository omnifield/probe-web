// Сборка — фабрика из зоны `build`. Панель стоит на той же базе, что и продукты:
// она девпанель, и показывать наше производство ей уместнее всего собой самой.
import { defineConfig } from "@omnifield/probe-web-build/vite";

export default defineConfig();
