// Модуль ради пробы дев-сервера: тянет подпуть соседа по воркспейсу, у которого есть и
// собранная пара, и исходник. В разметку приложения он не входит и в сборку не попадает —
// дев-сервер трансформирует его по запросу, как это делает браузер.

export { origin } from "probe-web-build-fixture-jsx-dep/built";
