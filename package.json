{
  "name": "probe-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "FORCE_COLOR=0 nx run-many -t build -p '@omnifield/*'",
    "test": "FORCE_COLOR=0 nx run-many -t test -p '@omnifield/*'",
    "lint": "FORCE_COLOR=0 nx run-many -t lint -p '@omnifield/*'",
    "typecheck": "FORCE_COLOR=0 nx run-many -t typecheck -p '@omnifield/*'",
    "dev": "nx run-many -t dev -p '@omnifield/*' --parallel=10",
    "graph": "nx graph",
    "nx": "nx"
  },
  "devDependencies": {
    "nx": "23.1.1"
  }
}
