{
  "name": "@omnifield/probe-web-ewc",
  "version": "0.0.0",
  "private": true,
  "description": "ewc — placeholder app, shown in the dev panel while the real shape is decided.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175 --strictPort --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . && pnpm run typecheck"
  },
  "dependencies": {
    "solid-js": "^1.9.14"
  },
  "devDependencies": {
    "@omnifield/probe-web-build": "workspace:*",
    "@omnifield/probe-web-lint": "workspace:*",
    "@types/node": "^24.10.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
