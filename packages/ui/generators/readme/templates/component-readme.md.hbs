# {{title}}

**Group:** {{group}} · **Genus:** {{genus}} · **Footprint:** {{footprint}}

## Passport

Runtime contract (`entity/passport.ts`) — parts, states, settings, exactly as the engine sees them.

### Anatomy

| part | meaning |
|---|---|
{{#each partMeanings}}
| {{part}} | {{meaning}} |
{{/each}}

### States

| part | state | mark | meaning |
|---|---|---|---|
{{#each stateRows}}
| {{part}} | {{state}} | {{mark}} | {{meaning}} |
{{/each}}

### Settings

| setting | meaning | default | mark |
|---|---|---|---|
{{#each settingRows}}
| {{setting}} | {{meaning}} | `{{default}}`{{#if dependsOn}} (depends on `{{dependsOn}}`){{/if}} | {{mark}} |
{{/each}}

{{#if variableRows}}
### CSS Variables

| part | variable | set by | meaning |
|---|---|---|---|
{{#each variableRows}}
| {{part}} | `{{variable}}` | {{setBy}} | {{meaning}} |
{{/each}}

{{/if}}
<!-- user:passport:start -->
_Nothing written here yet — this section survives regeneration; everything above it does not._
<!-- user:passport:end -->

## Data contract

What an assembly's `bind`/`repeat` paths actually point into (`entity/io.ts`) — separate from the look, the same input can be dressed by any recipe.

{{#if hasIo}}
{{#if inputSchemaJson}}
### Input

```json
{{inputSchemaJson}}
```

{{/if}}
{{#if outputSchemaJson}}
### Output

```json
{{outputSchemaJson}}
```

{{/if}}
{{else}}
_No `entity/io.ts` yet — this component has no data contract of its own to show._

{{/if}}
<!-- user:io:start -->
_Nothing written here yet — this section survives regeneration; everything above it does not._
<!-- user:io:end -->

## Components

Real Solid implementations, one per anatomy part (`components/kit.tsx`) — what actually draws each part, not just its name in the passport.

{{#if componentRows}}
| part | drawn by |
|---|---|
{{#each componentRows}}
| {{part}} | `{{component}}` |
{{/each}}

{{else}}
_No `components/kit.tsx` yet._

{{/if}}
<!-- user:components:start -->
_Nothing written here yet — this section survives regeneration; everything above it does not._
<!-- user:components:end -->

## Assemblies

Worked `RenderTree` trees (`playground/assemblies/`) — structural skeletons proving the passport composes, not the primary way to use the component (plain JSX composition is).

{{#if assemblyRows}}
{{#each assemblyRows}}
### {{name}}

{{means}}

```
{{outline}}
```

{{/each}}
{{else}}
_No assemblies declared yet._

{{/if}}
<!-- user:assemblies:start -->
_Nothing written here yet — this section survives regeneration; everything above it does not._
<!-- user:assemblies:end -->

## Recipe (proof only)

Proves the passport CAN be dressed by the real skin mechanism (`playground/recipe.ts`) — never ships as-is; a real look for this component lives in `packages/skin`, not here.

{{#if hasRecipe}}
{{#if recipeVariants.length}}
Variants: {{#each recipeVariants}}`{{this}}`{{#unless @last}}, {{/unless}}{{/each}}{{#if recipeDefault}} (default: `{{recipeDefault}}`){{/if}}
{{else}}
No named variants — this proof recipe carries no `data-variant` axis of its own.
{{/if}}

{{#if recipeSettingRows}}
Also conditioned by the component's own settings:

| setting | conditions styled |
|---|---|
{{#each recipeSettingRows}}
| {{setting}} | {{conditions}} |
{{/each}}

{{/if}}
{{else}}
_No `playground/recipe.ts` yet._

{{/if}}
<!-- user:recipe:start -->
_Nothing written here yet — this section survives regeneration; everything above it does not._
<!-- user:recipe:end -->

## Notes

<!-- user:start -->
_Nothing written here yet — this section survives regeneration; everything above it does not._
<!-- user:end -->
