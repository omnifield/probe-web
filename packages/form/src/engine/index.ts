// Единственная точка резолва `@tanstack/solid-form` в web-core — приложение никогда не
// импортирует вендора напрямую, по тому же образцу, что `@web-core/router`/`@web-core/query`.
export * from "@tanstack/solid-form";
