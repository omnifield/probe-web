{
  "//": [
    "Builds TYPES. `vite build` builds the JS (library mode, the `build` zone's factory) — only",
    "declarations are left here: the `.d.ts` files are built by the same compiler that checks the",
    "code, and there is no second source of truth about types in the package."
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "emitDeclarationOnly": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
