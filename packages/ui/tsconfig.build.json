{
  "//": [
    "Сборка ТИПОВ. JS обеих веток поставки собирает `scripts/build.mjs` (esbuild), потому",
    "что `tsc` не умеет Solid-трансформацию JSX — она компиляторная, а не рантаймовая.",
    "Здесь остаются только декларации: `.d.ts` собирает тот же компилятор, что проверяет",
    "код, и второго источника правды о типах в пакете нет."
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "emitDeclarationOnly": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"],
  "//exclude": [
    "Пробы компонента живут в его папке, но поставкой не являются: декларации на них",
    "уехали бы в `dist` и объявили бы наружу то, чего в поверхности пакета нет.",
    "`*.live.test.ts` (PWEB-111) — тот же довод, и отдельный шаблон ей нужен по другой причине:",
    "она импортирует общие фикстуры из `test/`, вне `rootDir` этой сборки, а не только несёт",
    "JSX — `tsc` иначе валит сборку `TS6059`, а не просто везёт лишнюю декларацию."
  ],
  "exclude": ["src/**/*.test.tsx", "src/**/*.live.test.ts"]
}
