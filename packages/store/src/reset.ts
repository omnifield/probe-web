// Аддон `@xstate/store`: `createStore({...}).with(reset())` — добавляет `store.trigger.reset()`,
// возвращающий контекст к исходному значению. См. довод по подпутям в `./persist`.
export * from "@xstate/store/reset";
