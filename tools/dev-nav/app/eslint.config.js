// Канон Solid машиной — пресет зоны `lint` (`@omnifield/probe-web-lint`).
//
// Пульт — оснастка локации, а не поставка, но код в нём такой же Solid, как у зон, и правила
// к нему те же. Подключается как ПОТРЕБИТЕЛЕМ: одной строкой, без правки правил. Понадобилось
// глушить правило пресета, чтобы написать обычный код, — это находка про пресет, а не повод
// завести здесь `rules`.
import { defineConfig } from "@omnifield/probe-web-lint";

export default [
  {
    // Сборка и зависимости — не наш код.
    ignores: ["dist/**", "node_modules/**"],
  },
  ...defineConfig(),
];
