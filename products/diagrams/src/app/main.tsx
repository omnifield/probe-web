// Точка входа приложения.
//
// Порядок тот же, что у скелета потребителя и у соседа `products/skin`: базовый слой значений,
// затем `mount` из зоны `runtime`.
//
// СВОЕГО ОФОРМЛЕНИЯ ЗОНА НЕ ЗАВОДИТ, тем же доводом, что и у скина: второй слой вида рядом с
// китом разошёлся бы с ним на первой же правке одного без другого. Базовый слой остаётся: он
// снимает браузерные умолчания. Цвет приходит со скином — или не приходит вовсе, и тогда кит
// голый.

import "@omnifield/probe-web-style/base.css";

import { mount } from "@omnifield/probe-web-runtime";

import { App } from "./app.jsx";

mount(() => <App />);
