// Перечень, из которого порождается CSS. Лежит ОТДЕЛЬНО от склейки намеренно: у настоящего
// соседа так и есть — данные живут своими модулями (`src/roles.ts`, `src/dimension.ts`), а
// склейка зовёт их из `src/css/generate.ts`.
//
// Проба правит именно этот файл, а не саму склейку: свежесть обязана держаться на ТРАНЗИТИВНОЙ
// правке. Правь она модуль, который дев-сервер грузит напрямую, — зеленела бы и та раскладка,
// где перечень остался вчерашним.

/** Метки, из которых собирается порождённый блок. */
export const MARKS = ["alpha", "beta"];
