// РАНТАЙМ-анатомия поверхности (`разнесено PWEB-127`) — первого компонента кита, у которого нет
// поведения вовсе.
//
// ЗДЕСЬ ТОЛЬКО ЧАСТИ И АДРЕСА — ничего больше, тем же приёмом, что у гармошки
// (`accordion/entity/anatomy.ts`). Полный рантайм-контракт — уровнем выше, в `passport.ts`.
// Срез РЕДАКТОРА — ещё шагом дальше, в `playground/index.ts`.
//
// Своих частей одна, и вложенные плоскости — не части: карточка внутри панели — это ДВА
// компонента, а не часть внутри части: обе плоскости самостоятельны, обе одеваются своим
// правилом, и вложенность у них ровно та, что в дереве. Заводить `surface.inner` значило бы
// объявить, что вложенная плоскость чем-то отличается от обычной, — а она не отличается.

import { createAnatomy } from "@omnifield/probe-web-skin/model";

/** Части поверхности. Она одна: плоскость — это один узел. */
export const anatomy = createAnatomy("surface").parts("root");

/** Адреса частей: `attrs` для узла, `selector` для стиля. Считаются один раз — они статичны. */
export const anatomyParts = anatomy.build();
