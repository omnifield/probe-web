// КОРНЕВОЙ ВХОД набора значений: всё, что есть в зоне, включая реактивную часть.
// Зависимостей на `ui`, `runtime` и `build` тут нет и быть не может: направление зависимостей
// одностороннее (`kb:PROBEWEB-4`).
//
// РАСКЛАДКА ВХОДОВ (`PWEB-44`). Их два, и различает их ровно одно — нужен ли Solid:
//
//   `@omnifield/probe-web-style`         — этот файл: значения, цвет И реактивный контроллер;
//   `@omnifield/probe-web-style/values`  — то же самое БЕЗ реактивной части, без Solid.
//
// Корневой вход = узкий плюс реактивное, и записано это здесь ОДНОЙ строкой `export *`, а не
// вторым перечнем имён. Причина не в краткости: два перечня разъехались бы, и однажды имя
// оказалось бы в корне, но не в узком входе — узкий стал бы беднее молча, а виноватого не
// найти. Перечень имён живёт в `src/values.ts`, там же и объяснения к нему.
//
// Отсюда же следует, чего здесь быть НЕ ДОЛЖНО: ни одного экспорта, кроме реактивного, мимо
// `values.js`. Появится — значит корень перестал быть суммой, и узкий вход обеднел. Держит
// это `test/pack.test.ts`, а не внимательность.
//
// ИНСТРУМЕНТОВ СТИЛИЗАЦИИ ЗДЕСЬ НЕТ (`PWEB-3`). `cn`, `createStyle` и реэкспорт `cva`
// уехали в самостоятельную необязательную поставку `@omnifield/probe-web-style-tools`, и
// зависимости на неё тут тоже нет: оформление вправе взять инструменты у нас, у кого-то
// ещё или не брать вовсе. Пока инструменты приезжали вместе со значениями, «необязательно»
// было словом — их привозила одна установка.
//
// CSS через этот вход НЕ идёт: манифест объявляет `sideEffects: false`, и импорт-побочка
// из корня была бы выброшена tree-shaking'ом при первом же неиспользованном экспорте.
// Стили едут отдельными подпутями — `/base.css` и `/themes.css`.

export * from "./values.js";

// РЕАКТИВНАЯ ЧАСТЬ — единственное, что этот вход добавляет к узкому, и единственное, ради чего
// в манифесте стоит одноранговый `solid-js`. Один файл (`src/theme.ts`), один импорт Solid.
export {
  createThemeController,
  registerTheme,
  type ThemeController,
  type ThemeControllerOptions,
  type ThemeMode,
} from "./theme.js";
