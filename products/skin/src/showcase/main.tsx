// Точка входа витрины.
//
// Порядок тот же, что у скелета потребителя: базовый слой значений, вид САМОЙ витрины, затем
// `mount` из зоны `runtime`.
//
// НАДЕВАЕМОЙ ПАЛИТРЫ ФРЕЙМВОРК БОЛЬШЕ НЕ ВЕЗЁТ, и это верно: палитра без рецептов — половина
// скина, а половин у скина не бывает. Базовый слой остаётся: он снимает браузерные умолчания и
// объявляет способность страницы к обеим половинам. Цвет приходит со скином — или не приходит
// вовсе, и тогда кит голый.
//
// Чего здесь намеренно НЕТ — импорта оформления кита. Скин надевается и снимается на живую
// механикой приложения, и показать надо именно это: без скина кит остаётся голым
// (`kb:PROBEWEB-11`, правило первое). Пока скина в зоне нет вовсе — витрина честно показывает
// голое.

import "@omnifield/probe-web-style/base.css";
import "./showcase.css";

import { mount } from "@omnifield/probe-web-runtime";

import { App } from "./app.jsx";

mount(() => <App />);
