// Ветка `default`: сюда разрешение попадает ТОЛЬКО если условие `solid` не сработало.
// Падаем на импорте — молчаливый проход мимо условия `solid` был бы худшим исходом:
// тест позеленел бы, ничего не проверив.

throw new Error(
  "probe-web-build-fixture-jsx-dep разрешился по ветке `default`: конфиг не добавил условие " +
    "`solid`, и зависимость отдала бы уже не JSX. Проверяй resolve.conditions в /vite и /vitest.",
);
