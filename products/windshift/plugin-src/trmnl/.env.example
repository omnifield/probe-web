# Copy to .env (gitignored) before running docker compose.
# Origin only — no /rest/api/v1 suffix.
WINDSHIFT_URL=https://windshift.example.com
# Profile > API Tokens. Needs items:read and users:read.
WINDSHIFT_TOKEN=crw_replace_me
