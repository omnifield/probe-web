// Пара к `generated-css-probe.ts`: CSS-подпуть ТОГО ЖЕ соседа, для которого функции-порождателя
// нет. Он обязан остаться файлом на диске — иначе объявление `./generate` перехватывало бы у
// пакета все его стили разом, включая написанные руками.

import "probe-web-build-fixture-jsx-dep/written.css";
