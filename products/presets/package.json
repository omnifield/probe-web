{
  "name": "@probe-web/presets",
  "version": "0.0.0",
  "private": true,
  "description": "Зона presets: служба, которая хранит непрозрачный JSON и отдаёт обратно. Формата фильтра НЕ понимает (kb:PROBEWEB-8). Приватная — в реестр не едет (kb:PROBEWEB-5).",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^7.0.2"
  }
}
