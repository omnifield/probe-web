// ПАСПОРТ компонента — то, что компонент объявляет о себе ДАННЫМИ, чтобы его можно было одеть
// скином и показать в редакторе (`tasker:PWEB-2`, страница раздела «Паспорт компонента»).
//
// ## Зачем он вообще
//
// Скин обещан «работающим в рамках контракта компонента», а рамки до сих пор негде было
// прочитать: имена зацепок жили в доке и в тестовом перечне, состояния — в голове у того, кто
// одевал, а варианты не существовали нигде. Скин при этом адресуется НЕ селектором, а
// координатами из паспорта (`часть` · `вариант` · `состояние` · `предок`), и селектор
// порождается механикой. Нет паспорта — нет и адресов: скину нечего перечислять, редактору
// нечего предложить, проверке «правило адресует несуществующее» не с чем сверяться.
//
// ## Три свойства, ради которых он такой, какой есть
//
//  1. **Едет в поставке — отдельным подпутём `@omnifield/probe-web-ui/passport`.** Всё, что не
//     доехало до потребителя, паспортом не является: разбор в доке и перечень в `test/` читает
//     только тот, у кого есть наши исходники, а скин и редактор их не видят.
//     Подпуть отдельный, а не корневой экспорт: паспорт — ДАННЫЕ, и читателю (механика скина,
//     редактор, чужой инструмент) не нужны ни JSX, ни `@kobalte/core`, ни Solid. Через корень
//     он тянул бы за собой всю поверхность примитивов.
//  2. **Форма одна на всех поставщиков.** Продуктовый пакет со своей таблицей объявляет себя
//     теми же типами и попадает в редактор на общих правах — паспорт это форма ПОСТАВКИ, а не
//     привилегия кита. Отсюда поле `package`: читатель обязан знать, откуда приехал компонент,
//     не зная заранее ни одного имени пакета.
//  3. **Объявляет имена и смысл, а не вид.** Здесь нет ни цветов, ни размеров, ни классов:
//     «у меня есть часть „бегунок“» и «у меня бывает намерение „опасное“». Как это выглядит —
//     дело скина и никем здесь не ограничено.
//
// ## Почему выписан руками, а не снят с исходников
//
// Снятый с кода перечень подтверждал бы сам себя: переименование части проехало бы вместе с
// правкой, прогон остался бы зелёным, а сломался бы вид у потребителя — после выпуска и
// молча. Поэтому паспорт объявлен здесь явно, а расхождение с кодом ловится проверкой в ОБЕ
// стороны (`test/passport.test.tsx`): объявленная часть обязана появиться в разметке, зацепка
// из исходника обязана быть в паспорте.
//
// Та же причина, по которой рядом живёт `test/slot-list.ts`, и оба перечня НЕ сливаются в
// один: слот-лист — обязательство по ИМЕНАМ на всю зону (не переименуем без мажора), паспорт —
// объявление КОНКРЕТНОГО компонента для скина и редактора. Пересечение проверяется (часть
// паспорта обязана быть обещанным именем), но предметы разные, и перечень зоны не становится
// паспортом от того, что в нём есть строка `button`.

/**
 * Чем состояние или намерение видно В РАЗМЕТКЕ.
 *
 * Без этого поля словарь состояний — просто слова: скин порождает из адреса селектор, и ему
 * нужно знать, за что цепляться, а редактору — чем выставить состояние в предпросмотре.
 *
 * Два начертания, и оба настоящие:
 *  • `attribute` — атрибут на узле (`data-disabled`, `aria-busy="true"`);
 *  • `pseudo` — псевдокласс, который ставит браузер (`:hover`, `:focus-visible`, `:active`).
 *
 * Псевдокласс не подменить атрибутом: наведения и клавиатурного фокуса компонент не знает —
 * их знает браузер, и объявить их иначе значило бы соврать.
 */
export type PassportMark =
  | {
      readonly kind: "attribute";
      /** Имя атрибута: `data-disabled`, `aria-busy`, `data-variant`. */
      readonly name: string;
      /** Значение, если состояние выражено именно им. Без значения — важно само наличие. */
      readonly value?: string;
    }
  | {
      readonly kind: "pseudo";
      /** Псевдокласс вместе с двоеточием: `:hover`. */
      readonly name: string;
    };

/**
 * Состояние части — момент, в который её вид отличается.
 *
 * Самая недооценённая половина паспорта: без объявленного словаря редактор не может предложить
 * «а теперь наведение», а скин не знает, что у части такое состояние вообще бывает.
 */
export interface PassportState {
  /** Имя из словаря — оно уезжает в адрес правила скина: `hover`, `disabled`. */
  readonly name: string;
  /** Что это значит человеку и редактору. Не вид. */
  readonly means: string;
  /** Чем выражено в разметке. */
  readonly mark: PassportMark;
}

/**
 * Намерение, с которым компонент ставится в разметку: основное действие, второстепенное,
 * опасное.
 *
 * **Объявляет их компонент, а не скин.** Имя варианта попадает в разметку ПОТРЕБИТЕЛЯ, и
 * принадлежи перечень скину — смена скина ломала бы разметку: вариант мог бы просто исчезнуть.
 *
 * Скин обязан одеть каждый объявленный вариант; чем именно он покажет опасность — заливкой,
 * контуром, чем угодно — его дело. А чисто визуальные имена (контур, призрак, мягкий)
 * вариантами компонента НЕ являются: это уже решение о виде, и живут они внутри скина как его
 * собственные разновидности одного намерения.
 */
export interface PassportVariant {
  /** Имя намерения — уезжает в адрес правила скина: `primary`, `danger`. */
  readonly name: string;
  /** Что оно значит. Намерение, а не вид. */
  readonly intent: string;
  /** Чем выражено в разметке. Ставит его ПОТРЕБИТЕЛЬ, кит пропускает насквозь. */
  readonly mark: PassportMark;
}

/**
 * Часть — именованное место компонента, за которое цепляется вид.
 *
 * Имя части совпадает с зацепкой `data-slot` не случайно: это одно и то же обещание, только
 * прочитанное с двух сторон. Корневая часть носит имя компонента, вложенные —
 * `<компонент>-<часть>`.
 */
export interface PassportPart {
  /** Имя части = имя зацепки `data-slot`. */
  readonly name: string;
  /** Назначение части — человеку и редактору. */
  readonly means: string;
  /** Словарь состояний ЭТОЙ части. */
  readonly states: readonly PassportState[];
  /**
   * Правило вложенности: какие ЧАСТИ этого же компонента допустимы внутри.
   *
   * Нужно редактору, чтобы он не дал собрать заведомо сломанное дерево.
   */
  readonly children: readonly string[];
  /**
   * Пускает ли часть содержимое потребителя (текст, значок, чужой компонент).
   *
   * `none` — место занято самим компонентом; класть туда своё нельзя.
   */
  readonly content: "consumer" | "none";
}

/** Паспорт компонента целиком — то, что читают скин и редактор. */
export interface ComponentPassport {
  /** Имя компонента. Оно же — имя корневой части. */
  readonly component: string;
  /** Откуда компонент приехал. Форма одна на всех, поэтому поставщик назван данными. */
  readonly package: string;
  /** Имя корневой части — с неё начинается дерево. */
  readonly root: string;
  /** Части. Каждая обязана появляться в разметке — это проверяется. */
  readonly parts: readonly PassportPart[];
  /** Намерения, которые скин обязан одеть. */
  readonly variants: readonly PassportVariant[];
}

/** Кто выдаёт паспорта этого пакета. Значение вынесено, чтобы не разъехалось по копиям. */
const PACKAGE = "@omnifield/probe-web-ui";

/**
 * Паспорт кнопки — первый и пока единственный (`tasker:PWEB-2`: механика обкатывается НА
 * кнопке, остальные компоненты получают паспорт по одному отдельными задачами).
 *
 * Состояний четыре, и три из них — псевдоклассы. Так и есть на самом деле: кнопка не хранит
 * ни наведения, ни фокуса, ни нажатия — их знает браузер. Единственное, что кнопка ПОКАЗЫВАЕТ
 * атрибутом, — отключённость: у отключённой кнопки нет ни `:hover`, ни `:active`, и без
 * `data-disabled` состояние снаружи было бы невидимо.
 *
 * Занятость (`busy`) объявлена, хотя атрибут ставит потребитель: кнопка «в работе» собирается
 * из готового (`<Button disabled aria-busy="true"><Spinner /></Button>` — проп-сахара `loading`
 * в ките нет намеренно), и не объяви мы этого, скин не смог бы одеть занятую кнопку ВООБЩЕ:
 * правило, адресующее необъявленное состояние, невалидно. То же и с вариантами — их пишет
 * разметка потребителя, а кит пропускает атрибут насквозь. Поэтому паспорт называет не только
 * имя, но и то, ЧЕМ состояние выражено: договориться об этом кроме как данными негде.
 */
const BUTTON: ComponentPassport = {
  component: "button",
  package: PACKAGE,
  root: "button",
  parts: [
    {
      name: "button",
      means: "кнопка целиком — один узел, по умолчанию нативный `<button type=\"button\">`",
      states: [
        {
          name: "hover",
          means: "указатель над кнопкой",
          mark: { kind: "pseudo", name: ":hover" },
        },
        {
          name: "focus-visible",
          means: "фокус пришёл с клавиатуры — обвод нужен, при нажатии мышью он лишний",
          mark: { kind: "pseudo", name: ":focus-visible" },
        },
        {
          name: "active",
          means: "кнопку держат нажатой",
          mark: { kind: "pseudo", name: ":active" },
        },
        {
          name: "disabled",
          means: "нажать нельзя; кнопка не зовёт обработчик",
          mark: { kind: "attribute", name: "data-disabled" },
        },
        {
          name: "busy",
          means: "работа идёт — атрибут ставит потребитель вместе с `disabled`",
          mark: { kind: "attribute", name: "aria-busy", value: "true" },
        },
      ],
      // Своих частей у кнопки нет: подпись, значок и индикатор кладёт потребитель, и это его
      // узлы, а не наши. Обещать за них нечего.
      children: [],
      content: "consumer",
    },
  ],
  variants: [
    {
      name: "primary",
      intent: "основное действие экрана",
      mark: { kind: "attribute", name: "data-variant", value: "primary" },
    },
    {
      name: "secondary",
      intent: "второстепенное действие рядом с основным",
      mark: { kind: "attribute", name: "data-variant", value: "secondary" },
    },
    {
      name: "danger",
      intent: "действие, которое трудно или нельзя отменить",
      mark: { kind: "attribute", name: "data-variant", value: "danger" },
    },
  ],
};

/**
 * Паспорта пакета по имени компонента.
 *
 * Перечень, а не «сколько их»: число устаревает при добавлении каждого следующего паспорта, а
 * форма чтения — нет. Компонент без паспорта здесь отсутствует ЧЕСТНО: его ещё не одевали, и
 * молчаливая заглушка выглядела бы как объявленный контракт.
 */
export const PASSPORTS: Readonly<Record<string, ComponentPassport>> = {
  [BUTTON.component]: BUTTON,
};

/**
 * Паспорт по имени компонента, либо `undefined` — если компонент его ещё не объявил.
 *
 * @param component имя компонента, оно же имя корневой части (`button`)
 */
export function passportOf(component: string): ComponentPassport | undefined {
  return PASSPORTS[component];
}
