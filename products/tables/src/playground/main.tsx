// Точка входа площадки. Состав вызовов тот же, что у эталона и у скелета потребителя:
// базовый CSS, тема, ОДЕЖДА, раскладка стенда, затем `mount` из зоны `runtime`.
//
// Третья строка — вся одежда разом. Таблица, отбор и график безголовые: без неё страница
// поднимется и будет работать, но останется голой. Одевает её ЧУЖАЯ зона, и это главное:
// свой вид рядом с ним был бы вторым источником правды о том, как выглядит таблица, а два
// источника расходятся тихо. Сборный подпуть тянет и набор значков — без него кнопки
// управления колонкой остались бы пустыми квадратами.
//
// Четвёртая — раскладка САМОГО стенда: сетка окна, сайдбар кейсов, шапка. Это утварь
// площадки, а не компоненты зоны, и одевать её некому.
import "@omnifield/probe-web-style/base.css";
import "@omnifield/probe-web-style/themes.css";
import "@probe-web/skin/tables.css";
import "./playground.css";

import { mount } from "@omnifield/probe-web-runtime";

import { App } from "./app.jsx";

mount(() => <App />);
