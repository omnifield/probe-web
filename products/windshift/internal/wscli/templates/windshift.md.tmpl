# Windshift

Use `ws` to manage work in **{{.Workspace.Key}}** ({{.Workspace.Name}}).

> Generated by `ws {{.CLIVersion}}` at {{.GeneratedAt}}. Refresh after CLI or
> workspace workflow changes with `ws config docs`.

## Working rules

- Follow user and repository instructions before this guide.
- When work refers to an item, read it before acting: `ws task get <KEY-123>`.
- Use item keys exactly as returned by `ws`; never construct a key from a
  numeric ID.
- Keep the item status accurate. Move it to an in-progress status when work
  starts and to a completed status only after the requested outcome is done.
- Record durable decisions, blockers, and completion evidence as item comments.
- Use `ws <command> --help` for operations not listed here.

## Work items

```bash
ws task mine                              # Items assigned to me
ws task created                           # Items I created
ws task get <KEY-123>                     # Details, parent, children, comments
ws task create -t "Title" [-d "Details"]
ws task edit <KEY-123> [-t "Title"] [-d "Details"] [--assignee <user-id>]
ws task move <KEY-123> <status>           # Change status
ws task parent <KEY-123>
ws task children <KEY-123>
ws comment list <KEY-123>
ws comment add <KEY-123> -m "Markdown body"
ws link ls <KEY-123>                      # Linked items, pages, and tests
```

Include the item key in commit subjects when the commit belongs to tracked
work, for example: `fix: handle empty input ({{.Workspace.Key}}-123)`.

## Attachments

Attach screenshots, logs, or generated files to an item rather than pasting
them into a comment.

```bash
ws attachment upload <KEY-123> shot.png [more.png ...]  # Attach local files
ws attachment list <KEY-123>                            # Attachment IDs and sizes
ws attachment download <attachment-id> [--to <path>]    # Fetch one back
```

## Workspace workflow
{{if .StatusAliases}}
Status aliases configured for this repository:

| Alias | Status | Command |
|-------|--------|---------|
{{- range $alias, $status := .StatusAliases}}
| `{{$alias}}` | {{$status}} | `ws task move <KEY-123> {{$alias}}` |
{{- end}}
{{end}}
Available item types:
{{range .ItemTypes -}}
- {{.Name}}
{{end}}
Available statuses:

| ID | Status | Category | Default | Completed |
|----|--------|----------|---------|-----------|
{{- range .Statuses}}
| {{.ID}} | {{.Name}} | {{.CategoryName}} | {{.IsDefault}} | {{.IsCompleted}} |
{{- end}}
{{if .HasTransitions}}
Allowed transitions:
{{if .InitialStatuses}}

Initial status: {{.InitialStatuses}}
{{end}}
| From | To |
|------|----|
{{- range .Transitions}}
| {{.From}} | {{.To}} |
{{- end}}
{{end}}

`<status>` accepts a configured alias, status name, unique partial name, or
status ID. Invalid moves report the allowed targets.

## Documentation

Search documentation before guessing at requirements. Search results contain
page metadata; fetch the selected page to read its Markdown body.

```bash
ws page search "keyword or phrase" [--limit 20]  # Search titles and content
ws page list [--label spec]                       # Browse visible pages
ws page get <page-id>                             # Read page Markdown
ws link ls page:<page-id>                         # Find linked work items
```

Follow repository documentation policy. If no more specific policy exists,
use Windshift Pages for durable shared documentation:

```bash
ws page create --title "Title" --file document.md
ws page edit <page-id> --file document.md
ws link add <KEY-123> page:<page-id>
ws page labels <page-id> [--add <label-id>] [--remove <label-id>]
```

## Workspace skills

Skills contain additional workspace-specific instructions. Load a skill only
when its name and description match the current task.

```bash
ws skill ls
ws skill get <skill-id>
```

## Test management

Use these only when the work item or repository workflow uses Windshift test
runs:

```bash
ws test case ls
ws test run mine
ws test run get <run-id>
ws test result <run-id> <case-id> passed|failed|blocked|skipped
```
