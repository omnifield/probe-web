# ❓ FAQ — грабли, на которые уже наступили

Пока пусто — компонент только начат (скелет, один `root`, без движка расчётов и без семьи
графиков). Заполняется тем же способом, что `table/FAQ.md`: не документация возможностей (та — в
`README.md`) и не план (тот — в `ROADMAP.yaml`), а конкретные ловушки выбранного движка/Solid/
skin-механики, каждая — по факту, после того как реально наступили на неё, не заранее.
