{
  "//": [
    "No `compilerOptions` of its own, same reason as every other app in the workspace: it must",
    "be typed by exactly what the `build` zone's preset says."
  ],
  "extends": "@web-core/build/tsconfig",
  "include": ["src", "vite.config.ts"]
}
