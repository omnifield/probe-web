import { defineConfig } from "vitest/config";

// Один проект, и это не упрощение, а свойство предмета: эталон — ДАННЫЕ. Ни документа, ни JSX
// здесь нет; всё, что о нём можно спросить, спрашивается у механики, которая превращает запись
// в текст. Живой вид проверяется настоящим браузером (`tools/live-check/`), а не jsdom: полосы
// прокрутки, системные элементы и каскадные слои он не рисует и не упорядочивает.

export default defineConfig({
  test: {
    environment: "node",
    include: ["test/*.test.ts"],
  },
});
