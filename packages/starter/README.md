# `@omnifield/probe-web` — заготовка фронта в `web/`

Обвес `baser`. Кладёт в чужую локацию **пять файлов скелета** под `web/` и больше ничего.
Вся начинка — за четырьмя пакетами, которые скелет зовёт зависимостями: рантайм, оснастка
сборки, примитивы и стилевой слой.

> Этот файл **едет в тарбол вопреки `files`**: npm всегда кладёт README, манифест и
> LICENSE. Значит правка README — это правка поставки, а не заметка для своих.

## Поставить

```sh
baser add @omnifield/probe-web
pnpm -C web install
pnpm -C web dev
```

Третья строка поднимает дев-сервер, `pnpm -C web build` собирает статику.

**`-C web` не опечатка.** Скелет ложится в `web/`, а не в корень: в одну локацию рядом с
нами встаёт обвес бэка (`core/`), и оба в корень означали бы перемешанные скелеты. Практическое
следствие для локации: `installCommand` девбокса обязан заходить в `web/` — в корне ставить
нечего.

## Что ложится

| файл | что в нём |
|---|---|
| `web/package.json` | имя приложения, четыре наши зависимости, `dev` и `build` |
| `web/index.html` | страница с `<div id="root"></div>`, точкой входа и блоком оформления кита |
| `web/vite.config.ts` | три строки: конфиг приезжает из `-build` |
| `web/tsconfig.json` | две строки: база типов наследуется из `-build` |
| `web/src/main.tsx` | CSS стиля, **названное место** для пресета и режима, примитив из `-ui`, `mount()` |

**Все пять — класс `placed-once`:** положены один раз, дальше файлы твои, и обвес в них
больше не заглянет. Правь свободно — при обновлении обвеса они не перезапишутся.

## Чего обвес НЕ трогает

`.gitignore` · `.devcontainer/` · корневой `README.md` · **корневой `package.json`** ·
`.omnifield/` · `.claude/`.

Корневой манифест назван отдельно, потому что соблазн залезть в него у фронта самый сильный —
там workspaces и скрипты. У файла один хозяин, и спор за него всплыл бы у потребителя.

## Настройки

Действуют **в момент укладки**, а не в момент работы: значение попадает в файл один раз и
дальше живёт как часть твоего кода.

| настройка | что делает |
|---|---|
| `productName` | поле `name` в `web/package.json` |
| `runtimeVersion` | диапазон npm для `@omnifield/probe-web-runtime` |
| `buildVersion` | диапазон npm для `@omnifield/probe-web-build` |
| `uiVersion` | диапазон npm для `@omnifield/probe-web-ui` |
| `styleVersion` | диапазон npm для `@omnifield/probe-web-style` |

Незаполненная настройка едет за нашими выпусками, заполненная замирает на твоём значении.
Версии в шаблоне не хардкодятся: дефолты проставляются перед публикацией, поэтому обвес не
ждёт выпуска ни одной из зон.

`@omnifield/probe-web-style` назван в манифесте **явно, а не транзитивно через `-ui`**:
приложение импортирует его CSS напрямую, а строгий менеджер пакетов транзитивный импорт не
разрешит. Это не избыточность.

Порта дев-сервера здесь нет намеренно. Он бы застыл в файле при укладке и выглядел живой
регулировкой, ничего не регулируя, — то, что крутится потом, живёт за пакетами.

## Как одеть безголовый кит

`@omnifield/probe-web-ui` **не везёт ни одного класса по умолчанию** (`kb:PROBEWEB-4`): он
отвечает за поведение, доступность и стабильные зацепки, внешний вид — твой. Поэтому скелет
показывает приём, а не оставляет тебя перед загруженной темой и неоформленной кнопкой на ней.

Приём — блок `<style>` в `web/index.html`, одно правило:

```css
[data-slot="button"] {
  border-radius: var(--radius-md);      /* производная шкала — из base.css */
  background-color: var(--primary);     /* значение — из themes.css */
  color: var(--primary-foreground);
}
```

Цепляйся за две вещи и только за них:

- **`data-slot`** — зацепка, которую примитив ставит ДО спреда пропсов, поэтому её можно
  перебить своим пропом. Одно правило одевает все кнопки приложения разом.
- **токены палитры** — `var(--primary)`, `var(--radius-md)`. Литеральный цвет не переключается
  вместе с палитрой и всплывёт светлым пятном в тёмном режиме. Разрешаются они только когда
  палитра НАЗВАНА: снимешь строку «названного места» — правило останется, а цвета из него
  уйдут. Имена — того **набора значений**, который подключён в `main.tsx`: наш стоит там
  умолчанием, но обязательным не является (`PWEB-3`). Взял значения на стороне — поставь в
  правило имена своего поставщика, оно твоё целиком.

Второй путь — `createStyle` из инструментов стилизации, когда класс зависит от состояния. В
скелете он не показан намеренно: вызов заморозил бы у тебя ещё одну точку поверхности ради
примера, а сами инструменты — отдельная необязательная поставка, и брать их ты не обязан.

**Оформление в разметке — место временное, и это названо в самом файле.** Своё оформление
клади отдельным `.css` рядом с `main.tsx`, импортируй его там, а блок из `index.html` убирай.

**Цена решения названа.** Пример замерзает у тебя навсегда — обвес в `placed-once`-файл больше
не заглянет. Поэтому он ровно один и держится пробой `test/styling.test.ts`: второй селектор
или литеральный цвет роняют прогон. Обратная сторона честного «ничего не показывать» была бы
хуже: скелет — первое, что видит чужая команда, и «как одеть безголовый кит» это первый же её
вопрос.

## Названное место: чем приложение одето

Одна строка в `web/src/main.tsx`, и она обязана быть:

```ts
restoreSkin({ presets: [DEFAULT_PALETTE] });
```

Она объявляет **пресет** (`data-theme` на `<html>`) и **режим** (класс светлой/тёмной пары там
же). До неё документ нецветной: палитра цепляется к документу только по имени, и `:root` не
красится ничем (`kb:PROBEWEB-18`). Zero-config снят сознательно — видимая строка лучше
невидимого умолчания.

Что вокруг неё стоит знать:

- **Имя — корню документа, а не контейнеру.** Производные шкалы и роли объявлены в `:root` и
  там же вычисляются; атрибут на `#root` поменял бы переменные, ничего не пересчитав
  (`kb:SKIN-7`, инвариант 1). Корень механика находит сама, в аргументах его нет.
- **`DEFAULT_PALETTE`, а не `"default"`.** Имя палитры принадлежит стилевому слою. Литерал в
  `placed-once`-файле — вторая копия чужого имени: переименуют её у себя, и приложение станет
  нецветным молча.
- **Свой пресет** — назови его идентификатор в перечне; порядок значим, первый считается
  умолчанием. **Пустой перечень** — законное состояние: встанет только режим, приложение
  останется на голом ките (инвариант 4).
- **Режим ортогонален пресету.** Один пресет живёт в обеих парах; «тёмного варианта пресета»
  отдельным пресетом не заводят (инвариант 3). Выбор человека механика помнит сама.

### Порядок импортов — не декоративный

```
базовый слой → ступени палитр → пресет оформления → оформление
```

Пресет, подключённый перед ступенями, проигрывает им в каскаде: получится вид, посчитанный от
умолчаний. Выглядит это как «скин почти работает» — хуже явной поломки, потому что чинить такое
никто не идёт. Слоты 3 и 4 в шаблоне закрыты комментарием: **оформление необязательно**, и
скелет без единого файла скина обязан запускаться.

Тот же случай ловит вторая строка механики, в конце файла:

```ts
checkStyleOrder({ marker: BASE_MARKER });
```

Она спрашивает документ, объявлен ли на корне маркерный токен подключённого набора значений, и
кричит в консоль, если пресет стоит, а базы нет. Ничего не роняет и ничего не красит; не нужна в
проде — сними строку. Имя маркера приезжает экспортом по той же причине, что и имя палитры.

### Набор значений — поставщик, а не фундамент

Ступени 1–2 даёт **поставщик значений**, и наш набор — один из них (`PWEB-3`). В скелете он
стоит умолчанием только потому, что приезжает зависимостью, а не потому, что без него что-то
ломается: палитрой вправе быть чужой набор, и приложение на нём — исправное приложение.

Поэтому наш набор занимает в `main.tsx` **три строки подряд, которые снимаются вместе**:

```ts
import "@omnifield/probe-web-style/base.css";
import "@omnifield/probe-web-style/themes.css";
import { BASE_MARKER, DEFAULT_PALETTE } from "@omnifield/probe-web-style";
```

Взял значения на стороне — сними их все три, подключи на их место файл своего поставщика,
назови его палитру строкой в «названном месте» (это ТВОЁ имя, литерал здесь законен) и реши
судьбу проверки порядка. Решений ровно два:

- **у твоего поставщика есть маркерный токен базы** — назови его имя вместо `BASE_MARKER`, и
  проверка продолжит ловить тот же дефект на его наборе. Имён она не знает: что передали, про
  то и спросит (`kb:PROBEWEB-13`);
- **маркера у него нет** — сними строку `checkStyleOrder(…)`. Спрашивать нечего, а оставленная
  проверка кричала бы «базы нет» на исправном приложении.

**Проверка при этом не выброшена, и выбрасывать её не за что.** Она ловит настоящий дефект:
пресет, вставший без базы, даёт вид, посчитанный от умолчаний. Условной стала не она, а её
вызов — до `PWEB-15` скелет подставлял туда наш маркер безусловно, и потребитель на чужой
палитре получал предупреждение о поломке на исправном приложении.

Половину блока снять не выйдет молча: `BASE_MARKER` и `DEFAULT_PALETTE` приедут ниоткуда, `tsc`
назовёт оставшиеся строки (TS2304), а собранное приложение упадёт на первой из них при запуске
(`vite build` типов не смотрит и такое соберёт). Громкий отказ вместо тихого ложного крика —
это и есть разница, ради которой блок сделан цельным.

**Это две РАЗНЫЕ точки.** Постановке пресета стили не нужны вовсе — её зовут как можно раньше;
проверке они нужны по определению — её зовут после монтирования. Слитые в одну, они тянут друг
друга в разные стороны.

### Вспышка темы: где мы честны про границу

Класс режима, вставший после первого кадра, даёт вспышку — пользователь видит светлую страницу
перед тёмной (`kb:SKIN-7`, инвариант 3). `restoreSkin()` для того и сделана синхронной: она не
знает про Solid, не трогает `#root` и не спрашивает вычисленные стили, поэтому её зовут первой
строкой точки входа — раньше монтирования и раньше твоего кода.

**Полной гарантии это не даёт, и мы говорим об этом прямо.** Модульный скрипт по стандарту
отложен: браузер вправе показать кадр раньше, чем он выполнится. Замер на собранном приложении
(Chromium, 2026-08-19): при быстрой раздаче класс встаёт на 27.6 мс против первого кадра на
36 мс — успевает; если отдачу бандла задержать на 1.5 с, документ всё это время стоит без
палитры и без класса режима, и только потом становится тёмным. Окно вспышки равно времени
загрузки бандла.

Железную гарантию даёт только обычный (не `module`) скрипт в `<head>` — он выполняется до
первого кадра. **Наша поставка такой строки не кладёт:** в ней стояли бы имена чужих зон
строками, замороженные у тебя навсегда. Файл твой — добавить её ты вправе, но цену за
разъехавшиеся имена платить будешь тоже ты.

## Что скелет замораживает

Файлы `placed-once` зовут пакеты вечно и той версией вызова, какой их положили. Поэтому
поверхность, которую трогает скелет, — **семь точек плюс `#root`** в разметке:

```ts
import { mount, restoreSkin, checkStyleOrder } from "@omnifield/probe-web-runtime";  // рантайм
import { Button } from "@omnifield/probe-web-ui";                    // примитивы
import { BASE_MARKER, DEFAULT_PALETTE } from "@omnifield/probe-web-style";  // имена стиля
import "@omnifield/probe-web-style/base.css";                        // стиль: базовый слой
import "@omnifield/probe-web-style/themes.css";                      // стиль: ступени палитр
import { defineConfig } from "@omnifield/probe-web-build/vite";       // сборка
// "extends": "@omnifield/probe-web-build/tsconfig"                   // типы
```

CSS двумя строками — не дубль: базовый слой не держит ни одного литерального цвета, значения
приходят ступенями палитр. Но **двух импортов мало**: с 2026-08-19 палитра цепляется к
документу только по ИМЕНИ, `:root` не красится ничем (`kb:PROBEWEB-18`), и документ, не
назвавший палитру, выходит нецветным — не сломанным (геометрия работает и без неё), а именно
нецветным. Имя называет строка «названного места», см. раздел ниже. Своя палитра вместо нашей —
поменяй идентификатор в перечне, файл твой.

**Три точки стилевого слоя — это точки НАШЕГО набора значений, а не условие работы скелета.**
Он умолчание, а не фундамент (`PWEB-3`, `PWEB-15`): взял значения на стороне — все три уезжают
вместе с ним, и поверхность скелета сокращается до четырёх. Как именно — раздел «Набор значений
— поставщик, а не фундамент».

**Корневой вход `@omnifield/probe-web-style` — седьмая точка, и добавлена она сознательно**
(`tasker:PROBEWEB-53`). Оттуда приезжают ровно два имени: идентификатор дефолтной палитры и имя
токена-маркера базы. Оба принадлежат стилевому слою, и строкой их в `placed-once`-файле держать
нельзя: переименование у соседа не доедет ни до одного уже созданного продукта.

Подпуть назван **с расширением**. Типы CSS у потребителя приходят шаблоном `declare module
'*.css'` из `vite/client`, а шаблон сопоставляется с ТЕКСТОМ спецификатора: `…-style/css` под
него не попадает, и `tsc` падает с `TS2882`. Дефект нашла зона `reference` до публикации —
иначе он замёрз бы в скелете каждого созданного продукта.

Версия Vite, набор плагинов, цель компиляции и способ разрешения путей за этими точками
двигаются выпуском пакета, не задевая уже созданные продукты. Так же уедет и переход на
Solid 2.0 со сменой `jsxImportSource`: у потребителя в `tsconfig.json` лежит только `extends`.

Число касаний сторожит проба `test/surface.test.ts`: попытка позвать из скелета что-то сверх
семи точек — красная.

## Проверить обвес

```sh
pnpm test               # объявление, груз, поверхность, оформление кита, названное место,
                        # поставщик значений
pnpm run typecheck      # типы проб
pnpm run check          # baser check . — обвес подходит посадочному месту
pnpm run payload        # baser pack . --into <временный каталог>
```

`baser check` отвечает данными и называет, чего не хватает, — проверяем им, а не глазами.

Каталог выдачи `pack` обязан лежать **вне** пакета и быть пустым: иначе нагрузка стала бы
частью того, из чего её собирают, а упаковка молча затёрла бы прежнюю. Поэтому `payload`
целится во временный каталог и чистит **свой собственный** перед сборкой.

## Форма объявления — 3

Блок `baser` в `package.json` объявлен формой **3**: это единственное значение, которое
проходит все ворота установленного `@omnifield/baser-cli@0.1.0`. Поднимать «заодно» нельзя —
отказ приедет не к нам, а к потребителю.

Обвес **ничего не исполняет**: он объявляет, а результат есть чистая функция от шаблона и
значений. На этом держится ответ `baser plan` «сошлось».

## Канон

`kb:PROBEWEB-4` (контракт семи зон и поверхности) · `tasker:PROBEWEB-1` (ТЗ на шов с `baser`) ·
`kb:BASER3-2` (у файла один хозяин) · `kb:BASER3-34` (обвес описывает себя сам) ·
`kb:BASER3-35` (генератор, но декларативный).
