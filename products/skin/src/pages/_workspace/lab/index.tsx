// МОК (`PWEB-173`, итерация 1 — проверка самого механизма роутинга, не содержимого страницы).
// Второй экран воркспейса, назначение ещё не объявлено — форма папки подготовлена заранее.
export function LabPage() {
  return <p>Лаборатория (мок) — маршрут "/lab" жив.</p>;
}
