// ПЛЕЙСХОЛДЕР — форма виджета подготовлена заранее (верх `WorkspaceRightbar`, над `Input`),
// содержимое (паспорт/сведения о показываемом компоненте) ещё не объявлено.
export function Info() {
  return <p>Инфо компонента (плейсхолдер)</p>;
}
