// Аддон `@xstate/store`: `createStore({...}).with(validateSchemas())` — рантайм-проверка
// событий/контекста по `schemas` из конфига стора. См. довод по подпутям в `./persist`.
export * from "@xstate/store/validate";
