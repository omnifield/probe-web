// Канон Solid, выраженный машиной, — пресет зоны `lint` (`@omnifield/probe-web-lint`).
//
// Зона `ui` — главный потребитель этого пресета в продукте: кит целиком состоит из работы с
// пропсами, а именно там реактивность и теряется. Проверка здесь не украшение прогона:
// `solid/no-destructure` и `solid/reactivity` ловят ровно тот класс ошибки, из-за которого
// «обёртка стала непрозрачной» — а тесты его видят не всегда, потому что первый рендер у
// сломанной реактивности правильный.
//
// Пресет подключён по относительному пути (`file:../lint` в манифесте): он ещё не
// опубликован, а ждать публикации ради собственной проверки значило бы её не делать. Это
// devDependency — в поставку не едет.

import { defineConfig } from "@omnifield/probe-web-lint";

export default [
  {
    // Сборка и зависимости — не наш код.
    ignores: ["dist/**", "node_modules/**"],
  },
  ...defineConfig(),
];
